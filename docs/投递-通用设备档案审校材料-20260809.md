# 投递：通用设备档案审校材料

日期：2026-08-09

行业：通用设备（`801072.SI`）

分支：`codex/general-equipment-review-delivery`

材料提交：`faea5c7a34afc5449834c0bc28a12ee61c22437d`

## 1. 投递结果

本批只投递公开审校材料，不修改生产数据库，不触发重新生成，不调用 review 接口。生产队列继续保持 `awaiting_review`，等待 Kimi 四尺审校意见通过公开镜像回投。

投递目录：`reports/evidence/industry-archive-review-20260809/`。

材料包括：

1. `通用设备-801072.SI-kimi-v2.2-初稿.md`：生产 `archive_queue.ai_draft` 全文，除末尾换行归一化外逐字一致；
2. `thermometer-snapshot-801072.SI.json`：2026-08-04 温度计正式快照中的覆盖信息和 `801072.SI` 行业对象；
3. `generation-evidence-801072.SI.json`：实际通道、模型、Prompt、联网搜索门禁、七节验证、耗时、token 留存情况和数据边界；
4. `submission-manifest.json`：三项材料的 SHA-256、本机材料提交哈希和 review 回投接口字段。

## 2. 生成事实

| 项目 | 记录 |
| --- | --- |
| 生产队列状态 | `awaiting_review` |
| 生成通道 | `kimi` |
| 实际模型 | `kimi-k2.6` |
| Prompt | `industry-archive-v2.2-web` |
| 开始时间 | `2026-08-07T23:34:47.604545+00:00` |
| 完成入库时间 | `2026-08-07T23:36:38.163474+00:00` |
| 队列墙钟耗时 | 110.558 秒 |
| `$web_search` | 已通过 fail-closed 搜索门禁 |
| 七节校验 | 通过，缺节 0，错误 0 |
| 核心股 | 5 只 |
| token usage | 旧适配器未持久化，无法恢复，证据中按 `null` 记录 |

`$web_search` 留痕的证据类型是“准入不变量”，不是历史 HTTP 原始 trace：当前生产代码只有在收到 `$web_search` 工具调用后才置 `used_web_search=true`；未搜索直答会被拒绝，只有越过搜索门禁的非空正文才可能写成 `awaiting_review`。历史请求没有持久化逐轮 HTTP trace、request id 或 usage，因此没有补造轮次和 token。

## 3. 一致性与边界

| 核验 | 结果 |
| --- | --- |
| 初稿与生产数据库一致 | 是 |
| 快照行业对象与 sidecar 原始 payload 一致 | 是 |
| v2.2 校验器重跑 | `ok=true` |
| 初稿 SHA-256 | `5132f8dcfd22896aeaeeb85a65f0056737c90b0233834feb19a83a20ac3dd015` |
| 快照 SHA-256 | `2c53a22ba803016e5616cf2e930636861db742a8b141715dde2365c8616f4e01` |
| 生成证据 SHA-256 | `64bc522afe66c443c5fe6266dc4a9fde6772b3a6ad1823727f4f8a476334de0a` |
| 生产数据库写入 | 0 |
| 生产生成调用 | 0 |
| 密钥值或响应正文进入生成证据 | 0 |

材料提交 `faea5c7a34afc5449834c0bc28a12ee61c22437d` 精确标识上述三项审校输入，供 review 接口或人工审校校验。公开镜像提交哈希在推送并全新克隆复核后回报；审校意见回投时应把该镜像哈希填入 `mirror_commit_hash`。
