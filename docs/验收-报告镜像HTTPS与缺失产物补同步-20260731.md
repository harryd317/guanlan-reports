# 报告镜像 HTTPS 与缺失产物补同步验收报告

- 日期：2026-07-31
- 镜像仓库：`https://github.com/harryd317/guanlan-reports.git`
- 结论：2054 个待同步文件已推送，首轮全新克隆验证通过
- 生产影响：无。本任务没有部署、重启、停止或修改生产服务

## 一、源提交与集成状态

| 项目 | 提交 | 验证 |
| --- | --- | --- |
| 镜像钩子环境污染修复 | `f8b97840e4cabbe8172dcf22b9cba152c05b75b3` | main 与 T7 的祖先 |
| HTTPS、代理继承、`AGENTS.md` 发布 | `27175b60716c611ac2665a0852abff1526f6517b` | main 的祖先；T7 已拣选 |
| 中文交付路径触发修复 | `c22767a` | 镜像测试 27/27 |
| T7 镜像验证报告 | `ba179ee` | 已提交到 `codex/t7-today-two-lines` |
| T7 当前源提交 | `f5ba705` | 含最终报告与中文路径修复 |
| S6 正式测量源提交 | `7b560d4759a6b49470e1966b575b02105fb69b59` | 已进入分支镜像 |

T7 没有合并 main，因为 main 含首次部署的回滚提交。本轮只把镜像发布修复拣选到 T7，没有改变生产历史。

## 二、HTTPS 凭据边界

初始 macOS Keychain 凭据属于其他 GitHub 账户，目标仓库因此返回 403。修复后的凭据边界为：

- 令牌类型：fine-grained Personal Access Token；
- 资源所有者：`harryd317`；
- 仓库范围：仅 `guanlan-reports`；
- 可配置权限：仅 `Contents: Read and write`；
- GitHub 必选权限：`Metadata: Read-only`；
- 有效期：30 天；
- 名称与描述：`mirror-sync`；
- 存储：macOS Keychain，通过 `git credential approve` 写入；
- 匹配：目标 HTTPS URL 启用 `useHttpPath`，用户名固定为 `harryd317`。

一枚曾进入对话的新令牌被视为已泄露并立即撤销；GitHub 显示它从未使用。替代令牌没有进入对话、命令参数、日志、文件或镜像。

## 三、推送证据

写权限探针使用临时提交 `13dc96b1acb12178ec437e7ef855a2343a4d90d8`，执行 `git push --dry-run origin HEAD:main`。GitHub 接受写操作，但该探针提交没有进入远端。

全量回填结果：

| 批次 | 源 | 文件数 | 镜像提交 |
| --- | --- | ---: | --- |
| 分支批次 | 13 个 `codex/*` 分支 | 1888 | `044947a3fac7e29b98c83f86eaf504e1eaa57168` |
| main 批次 | main 根目录 | 166 | `36b76e5c5877178fad831dea91464d640850ef1f` |
| 合计 | main、T7、S6 及其他可发布分支 | 2054 | 以 main 批次后的 `HEAD` 为准 |

两次推送的 `blocked_findings` 都为 0。发布白名单仍只包含 `docs/`、`reports/` 和顶层 `AGENTS.md`；`MEMORY.md`、代码、数据库、日志、备份和密钥文件均不发布。

## 四、首轮全新克隆验证

从 HTTPS 远端克隆到新临时目录后，取得以下证据：

- 远端 `HEAD`：`36b76e5c5877178fad831dea91464d640850ef1f`；
- 跟踪文件：2203；
- 工作树：清洁；
- T7 验收 JPEG：10 张；
- 必查路径：8/8 存在；
- 敏感信息扫描：`blocked_findings=0`。

必查路径包括：

```text
AGENTS.md
branches/t7-today-two-lines/AGENTS.md
branches/t7-today-two-lines/docs/superpowers/specs/2026-07-31-t7-production-equivalent-remediation-design.md
branches/t7-today-two-lines/docs/事件记录-T7部署回滚-20260731.md
branches/t7-today-two-lines/docs/验收-T7-生产等效环境修复-20260731.md
branches/s6-screen-measurement/AGENTS.md
branches/s6-screen-measurement/docs/测量报告-S6-初筛有效性-20260731.md
branches/s6-screen-measurement/docs/验收-S6-初筛有效性测量-20260731.md
```

首轮克隆还暴露了一个结构细节：main 的 `reports/` 当时为空，Git 不跟踪空目录，因此完整结构验证器在新克隆中报告缺少根 `reports/`。本报告同时新增 `reports/report-mirror-https-backfill-20260731/verification.json`，使最终镜像保留可验证的根报告目录。

## 五、中文交付路径钩子修复

T7 最终报告提交时，钩子错误跳过自动同步。根因是 `git diff-tree` 默认把中文路径编码为带引号的 C 风格转义字符串，触发器因此无法识别 `docs/` 前缀。

TDD 证据：

1. 新增真实 Git 仓库测试，提交 `docs/验收-T7.md`；
2. RED 稳定返回带引号的八进制转义路径，`should_sync_paths` 结果为 false；
3. 最小修复把 `diff-tree` 改为 NUL 分隔的原始路径输出；
4. 新用例、旧工作树钩子集成用例和镜像全套测试均通过，总数 27/27。

## 六、最终验证门禁

本报告和结构化证据提交后，必须再次通过 HTTPS 同步 main 和所有 `codex/*` 分支，然后在另一个全新克隆中重跑以下检查：

1. 远端 `HEAD` 等于同步脚本回传的完整哈希；
2. 本报告和 `verification.json` 存在；
3. T7、S6 和 `AGENTS.md` 的必查路径仍存在；
4. T7 十张截图的数量、尺寸和 SHA-256 不变；
5. 镜像结构验证和敏感信息扫描均通过。

镜像提交哈希是本报告所在镜像提交的内容地址，无法同时自嵌到该提交中。因此，本报告记录首轮全量回填哈希，最终完成回执必须以报告同步后的远端新克隆 `HEAD` 为准。
