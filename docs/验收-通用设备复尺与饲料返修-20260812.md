# 验收：通用设备复尺与饲料返修（2026-08-12）

## 结论

- 通用设备（801072.SI）复尺意见已经镜像回投并写入生产四尺字段。生产队列保持 `awaiting_review`，页面语义为待段睿终审；`review_json` 已存在，`archive_id=null`，没有发布。
- 饲料（801014.SI）四尺意见已经镜像回投；三项小修已完成，返修稿 `validate_archive_v2` 为 `ok=true`。返修稿已在镜像新克隆验证，生产队列停在 `awaiting_review` 等待再次审校，没有发布，也没有触发 AI 重新生成。

## 审校原文回投

两份原文的源提交为 `ded31b810bb0891525c948b9550377886d505aa3`，审校原文镜像提交为 `575ee8cb67ee972f9bb80ee84abfb21091dd49c4`。全新克隆后逐字比较通过。

| 文件 | SHA-256 |
|---|---|
| `review/通用设备-复尺意见-20260812.md` | `635efeda0312461015dba1a2c304d583e0a4d7a631ad112144e5759820903a1f` |
| `review/饲料-四尺审校意见-20260812.md` | `4868b84d0ce549015a83a8bfed48facd980ebe136e710754f9d1f456bb914097` |

## 通用设备生产状态

- 四尺结论：`结论：通过，可进入段睿终审。`
- `status=awaiting_review`
- `mirror_commit_hash=575ee8cb67ee972f9bb80ee84abfb21091dd49c4`
- `review_json` 已存在，审计数由 2 增至 3，末次动作为 `external_review`。
- `reviewed_at=null`、`archive_id=null`；没有调用 `publish_reviewed_archive`。

## 饲料返修

只改三行正文：

1. ③节删去“2025H2猪价有望18-20元/kg”，保留“2025H1猪价约14.6元/kg（历史参考）”。
2. ②节补全主语为“海大集团：越南/印尼市占率15%/8%”。
3. ①节量比改为快照全精度 `0.9852181371622244`。

返修稿源提交为 `70a9b16c8fde8956839082b199f2086fc69a4061`，返修材料镜像提交为 `911903e8ad83ebc3855b1fdb5f600b6ce18584ad`；全新克隆已验证以下三个文件：

- `reports/evidence/industry-archive-review-return-801014-20260812/饲料-801014.SI-kimi-v2.2-返修稿.md`
- `reports/evidence/industry-archive-review-return-801014-20260812/change-notes-801014.SI.md`
- `reports/evidence/industry-archive-review-return-801014-20260812/validation-801014.SI.json`

返修稿 SHA-256 为 `0f51e7bdbba36a702b490e81d5297a27b38c793ec21fa87371abc50bdf02b2ef`。

### 校验与不变证明

| 项目 | 结果 |
|---|---|
| `validate_archive_v2.ok` | `true` |
| 缺失章节 | `[]` |
| 错误 | `[]` |
| 核心股 | `5` |
| ①节剔除量比后的前后 SHA-256 | 均为 `023f7b9a6f75d1db724fe8e49093cecdfbf4d8c399bbc5cb33439a445edad7af` |
| 生产骨架前后 SHA-256 | 均为 `b59c07708b3961d8b8c428625329947dd1fc24674f2e119d22ad3aafd09f1bc6` |
| 温度计 sidecar 前后 SHA-256 | 均为 `e4ea0f2354180c5f815e1b996be9dba935f21bd95d547baf817c4e9cb27f794e` |

## 生产写入边界

- S8 生产库任务前 SHA-256：`03a6d969c9d7a8a60f4511494007d387d46c3e5ba88cffb0ce47d4bc9cc06a53`。
- 通用设备复尺写入后：`efe1116e4a3f51c3a75b25245989b31a3c63e4f3c4cb39eca48cb1715fb7b174`。
- 全部队列流转后：`d7e12951dffb88ed986c12eed6a2f19f1acb1766d68219422d4d249fe5ce381a`。
- 写入仅涉及 `archive_queue` 的 `801072.SI` 和 `801014.SI` 两行；未写其他表。
- 温度计 sidecar 只读，哈希未变；没有调用档案发布函数，没有触发 AI 生成。
- 生产健康检查 `ok=true`，启动与磁盘指纹一致，规则版本 10。

## 回归

| 套件 | 结果 |
|---|---:|
| `test_rules.py` | 8/8 |
| `test_rules_mid.py` | 20/20 |
| `test_regime.py` | 6/6 |
| `test_industry_archive_center.py` | 35/35 |
| `test_report_mirror_sync.py` | 30/30 |
| `test_web.py` | 838/838 |

`test_regime.py` 如实提示：隔离工作树没有 `market.db`，三项仅供数据机执行的历史案例跳过；其余 6 项断言全部通过。

最终源提交和最终镜像提交以本报告提交后、远端全新克隆复核所得哈希为准。
