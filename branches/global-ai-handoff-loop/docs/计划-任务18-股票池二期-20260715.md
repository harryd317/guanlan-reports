# 任务18 · 股票池二期(一页三视图,收编 /pro 专业表格)实施计划

> **执行方式:** 子代理 TDD 逐任务执行,任务间双审(规格+质量),完工整支线终审——同任务17流程。
> **原型(已拍板,视觉唯一依据):** `docs/原型-股票池二期-20260715.html`(定稿含用户唯一修改:筛选结果默认折叠)

**目标:** /pool 重构为「股票池」一页三视图——①我的池子(状态机,唯一可操作) ②盘中监控(84只,实时只读) ③筛选结果(收盘流水,只读+立即重筛);/pro 原页保留不拆。

**架构:** 纯呈现层。pool.html 单文件重写;AI 二审抽屉抽成共享资产 static/ai_drawer.js(仿 nav.js 先例);service.py 仅加一条只读路由;所有数据走既有端点(/api/pool、/api/intraday、/api/dashboard、/api/rescan、/api/scan_status),后端业务零改动。

## 全局约束(每任务默认适用,同任务17)

- 判定链四文件+strategies/*零改动;test_rules/test_web 既有用例只加不改
- 铁律6:盘中措辞只许「临近/等收盘确认」,盘中破位样式只在盘中出现;判定结论文案(回调中/尚未回踩/否决原因)照抄零改动
- **状态机语义一字不动**:doTransition/submitBuy/toggleBuy/替换流程/countdownText/confirm二次确认/need_replace/thesis_generating/position_created 全部原逻辑保留,只换壳
- 改 .py 必 ./restart.sh;只改 static 免重启;三测+test_regime 收尾必跑
- **grep 守卫**:重写 pool.html 前先 `grep -n "_src_pool\|pool.html" test_web.py`,被旧锁(T5F1等)钉住的字符串以隐藏死DOM等价保留
- **大文件施工铁则(任务17-3教训)**:严禁一次性生成整个新文件——先写骨架,再分区块小步 Edit(单次≤150行),步间跑 test_web
- **E2E 禁改库**:验收只做 tab切换/折叠/渲染/买入表单开合,绝不点 立项/入池/否决/标记买入确认/移出/出场(生产库!)

---

### 任务18-1: AI二审抽屉共享资产 ai_drawer.js

**Files:** Create `static/ai_drawer.js`;Modify `service.py`(仿 nav_js 加 `/ai-drawer.js` 路由);Test `test_web.py` ㉗节 T18D×3
**Interfaces:** Produces `window.aiDrawer.open()`;自注入抽屉 DOM+样式(仿 nav.js);端点逻辑从 index.html JS 548-739 + HTML 204-218 **逐字迁植**(今日二审 /api/review/today、对话 /api/review/chat、复制快照 /api/context_text、协议卡 /api/rule_proposal、/api/memory/append);/pro 本期不动(保留其内联版,与共享版共存,/pro 退役时一并清,注释说明)

- T18D1 ai_drawer.js 存在且含五端点字符串+window.aiDrawer;T18D2 service 挂 /ai-drawer.js 路由(inspect.getsource);T18D3 pool.html 接入(script src + 按钮调 aiDrawer.open)——T18D3 在任务18-2 落地后才转绿,先写入允许 FAIL 记录 RED
- 步骤:测试先行→写 ai_drawer.js(自包含:injectStyle+injectDOM+bind)→service.py 路由→./restart.sh→curl /ai-drawer.js 200→三测(T18D3 允许红,其余全绿)→commit `任务18-1: AI二审抽屉共享资产`

### 任务18-2: pool.html 重构为一页三视图

**Files:** Modify `static/pool.html`(重写);Test `test_web.py` ㉗节 T18A×5+T18B×5+T18C×5
**Interfaces:** Consumes 18-1 的 aiDrawer;一期 nav.js(pool 页已接入,保留);/api/dashboard 一次 fetch 共用(视图① pos_alerts + 视图③ groups/fails/fresh)

**布局映射(原型→实现):**
1. 页头:h1 股票池 + AI二审按钮(调 aiDrawer.open);seg 三tab(我的池子N/盘中监控N/筛选结果·宇宙N),`?view=pool|intraday|scan` 深链,默认 pool;每视图头部一行口径小字(三个数字语义,文案照原型)
2. **视图①我的池子**:statbar 五态计数+9/30;五分区卡片栅格(研究中/观察池/持仓/候选冷却空态收窄一行);卡片=一期设计语言,kv 四列(收盘/平台止损/**离买点差**(watchlist)或**距止损**(holding)/七因子);观察池卡加「📡技术面一句话」(join /api/intraday:next_hint 或 near→回踩临近 + 今日pct + 量比,盘中加"(盘中只提示,等收盘确认)");持仓卡加规则5告警行(snapshot.pos_alerts join,无告警绿✓);操作按钮/买入表单/替换区原逻辑原文案
3. **视图②盘中监控**:index.html renderIntraday(445-489)整体迁植——独立轮询(盘中50s/收盘120s)、三态徽章(盘中·自动刷新/午间休市/已收盘·{date}收盘数据)、nearrow/belowrow 只在盘中、held 行「持仓」角标、hint 原文
4. **视图③筛选结果**:freshrow(数据截至/完成于/耗时+**立即重筛按钮**,进度条+scan_status 轮询迁植)+ 五折叠段(Ⓑ回踩到位/Ⓒ新突破/Ⓓ跟踪中(小字标注"技术口径,≠严选池观察")/Ⓔ否决/取数失败),**默认全部折叠**(拍板),表格列名列序照 /pro 原样
5. 移动端≤680px:seg 换行、表格容器 overflow-x、卡 kv 两列;底部Tab由 nav.js 负责

**测试(先行落 ㉗ 节):**
- T18A:①seg三按钮+v0/v1/v2容器 ②三处口径小字("严选池在册"/"两套口径"/"筛选宇宙") ③默认视图pool+深链view= ④AI二审按钮接 aiDrawer ⑤立即重筛只在视图③
- T18B:①doTransition+/api/pool//transition 仍在 ②submitBuy+成交价/日期/金额三字段 ③confirm 二次确认仍在 ④need_replace/替换区字符串 ⑤thesis_generating/position_created 提示
- T18C:①「盘中破位(等收盘确认)」在+「已触发」「已确认」不在 ②折叠段默认收起标记 ③watchlist区「离买点差」在且「距平台」不作列名(否决原因原文里的"距平台+25%"是判定结论,允许存在——断言限定在 kv/表头) ④持仓卡含"规则5" ⑤freshrow 三要素
- 施工顺序:测试→骨架(页头+seg+三空容器+深链)→视图①(分区逐块)→视图②迁植→视图③迁植→移动端→grep守卫复核→三测→commit `任务18-2: 股票池一页三视图`

### 任务18-3: E2E 验收 + 交付

- 三测+test_regime 终跑;./restart.sh(18-1 改过 service.py 的终态校验)
- 浏览器 E2E(**禁改库**):/pool 三视图切换/折叠开合/买入表单开合(不提交)/盘中监控表渲染/375px 三视图;/pro 回归一眼(未被拆坏);首页右栏「全部→」落到 /pool 视图①
- 截图汇报;CLAUDE.md 补任务18段;`docs/交付总结-18-股票池二期-20260715.md`(含遗留清单);commit+push

## 附:侦察结论索引
- /pro 全区块与 /pool 状态机字段/操作/端点契约:侦察报告已核(a0fcf…),关键结论已内嵌上文
- 三数字口径:10=严选池在册五态合计 / 84=focus+breakout+tracking+持仓 / 253=universe_n 扫描宇宙
- AI二审五端点与抽屉行为:index.html 204-218(HTML)+548-739(JS)
