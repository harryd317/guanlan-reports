# 任务16 · 个股全景页 — 实现计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task.

日期:2026-07-10 · 拍板(细化原型 docs/原型-个股全景页-20260710.html):①七区结构照原型;②行业深析=点按钮生成+当日缓存;③全站票名统一可点。

**关键落地决策**:不新建页面——现有 `/stock/{code}`(stock_detail.html,任务5)已具备七区中五区(关键位速览/K线/论点档案+基本面跟踪/事件时间线/AI对话),且 daily/pool/index 已链向它。本任务=**升级现有页**(补三段课+行业深析+锚点导航+白话化)+ 简明模式入口补全(home/ask)。旧能力零删除。

## Global Constraints

- 判定引擎零改动;AI 新调用(行业深析)必须:按需触发+每票每日缓存+与课文同款护栏(引擎不可用给人话降级);
- 缓存表通用化:`ai_docs(code, d, kind, content, created_at)`,UNIQUE(code,d,kind)——行业深析 kind='industry_deep',将来课文类产物可复用;
- 完工三测全绿(399→405±)+ E2E + kickstart 生产。

## Task 1: 行业深析引擎(reviewer)+ 缓存表(storage)——先红后绿 T17A/B

- `storage`:建 ai_docs 表(建表区+老库迁移惯例);`ai_doc_get(code, d, kind)` / `ai_doc_save(code, d, kind, content)`(INSERT OR REPLACE);
- `reviewer.industry_deep(code, thesis, events, profile)`:五节结构提示词(①什么生意钱从哪赚 ②行业位置 ③政策等关键变量 ④竞争格局它排第几 ⑤对这只票意味着什么),白话、引用注入数字、没有的明说;_run_lock 并发1;返回 {ok, content} / None(引擎不可用);log_ai_usage("industry_deep") try 包裹(T16 教训);
- T17A:mock 引擎断言五节输出/引擎死 None;T17B:ai_docs 存取+同键覆盖幂等。

## Task 2: 端点(service)——T17C

- `GET /api/industry_deep/{code}`:当日缓存命中直接返回 {ok, content, cached:true};`?generate=1` 时未命中则现场生成(拉 thesis/events/profile 注入)并落缓存;引擎不可用 {ok:false, error:人话};
- T17C:mock reviewer.industry_deep,断言缓存命中不再调引擎、generate 生成并落库、无缓存且不带 generate 返回 {ok:true, content:null}(前端据此显示按钮)。

## Task 3: stock_detail.html 升级——T17D

- 顶部加吸顶锚点导航(现状/K线/三段课/档案/大事记/行业深析/追问,滚动定位);
- 「关键位速览」区补充:体检口径 facts(/api/stock_test 现成:现价/平台/距平台/生命线信息在 reason)+当日复核建议(GET /api/daily/latest 该票卡的 ai_verdict,有则渲染徽章+理由,无则不占位);
- 新增「AI 三段课」区:fetch /api/lesson/{code}(现有缓存),三段渲染;失败降级"课文暂不可用(仅池内票提供)";
- 新增「行业深度解析」区:有当日缓存直接全文渲染;无则蓝按钮「生成行业深析(约半分钟)」→ GET ?generate=1 → 渲染+标注"今日缓存";
- 标题白话化:AI 二审员→就这只票追问(tag 保留)、论点档案→深度档案(入池研究报告);其余区名对齐原型;
- T17D:源码断言(锚点导航/lesson 接线/industry_deep 接线/ai_verdict 渲染/白话标题)。

## Task 4: 简明模式入口补全——T17E

- home.html:卡片票名(cardHtml title)、持有列表票名、等买点票名 → `<a href="/stock/{code}">`(样式:继承色+下划线点提示,不破坏现有布局与点击区);
- ask.html:课文卡标题旁加「全景 →」链接(池条按钮不动,它有自己的选票交互);
- T17E:两文件源码断言。

## Task 5: 三测 + E2E + 审查 + 上线

- 三测全绿;E2E(8788):打开 /stock/605117 断言七区渲染、锚点跳转、行业深析按钮→生成(mock 或真实一次)→当日缓存二次进入直接显示、home 票名链接可点;
- 审查 workflow(AI链/缓存/前端回归)敌对验证,确认项全修;
- kickstart 生产+curl 验收;交付总结+CLAUDE.md/MEMORY.md。

## Self-Review

拍板三点:七区=旧五区升级+新两区(Task 3);按需+缓存(Task 1/2);全站入口(Task 4:home/ask 补全,daily/pool/index 本就有)。类型 ai_docs(code,d,kind,content) 贯穿;lesson 池外降级;成本护栏对齐既有惯例。
