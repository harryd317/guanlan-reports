# 任务7 完成报告(简明模式:两键界面 + 白话翻译层 + AI课堂 + 微信推送)

> 交付时间:2026-07-07(上海)。已上线:8787 首页即「今日助理」。
> 开发方式:subagent-driven——9 个任务包,每包独立实现代理(严格 TDD)+独立审查代理
> (Spec+Quality 双结论),不过关就修复+复审;全部完成后再跑 107 代理的最终整体审查
> (5镜头×每发现3票对抗),确认 15 个跨包集成缺陷并全部修复。
> 全程台账:logs/task7-progress.md;给 Hermes 的详版总结:docs/项目总结-给Hermes-20260707.md。

## 做了什么

**新增四个文件(简明模式本体):**
- **brief.py(472行)**:决定性白话翻译层(纯函数,不碰网络不调AI)。
  黑话词典 STATE_CN + condition_cn(E1|→跌破止损线:、依据T1→依据落袋纪律、周MA30→30周生命线…);
  sell_line(卖出线=有效止损/锁盈线/生命线三者取最高,锁盈比例现读 CONFIG_MID 可调参数);
  suggest_position(账户2%风险倒算+30%仓位帽+100股取整);build_brief(日报+池+快照→五类消息卡,
  **与池现状对账**:candidate 强制出新机会卡、已买入/已卖出的卡自动消失;E2/E3 离场课按依据分讲,
  不再张冠李戴)。
- **push.py(123行)**:微信推送(Server酱优先/企业微信备选)。标题件数=实际可渲染行数
  (与首页口径一致);PUSH_QUIET 无事日静默可开关;钥匙只报在否、先消毒后截断、
  外层异常只记类型。
- **static/home.html(401行)**:「今日」页(新首页)。秘书消息卡(卖出红/落袋金/买入蓝/
  新发现灰/复核黄)+headline+学习区+一键记账按钮(在途禁用防连点)+持有列表(卖出线+红绿灯,
  breakeven/profit 两档文案)+等买点+底部折叠;全部卡片处理完显示「今天的事都处理完了 ✅」。
- **static/ask.html(523行)**:「问AI」页。股票池=课程目录(人话状态),点票出三段AI课文
  (行业/技术面/基本面,LESSON_SEQ 代次防串票)+真实K线嵌课文+就票追问+行业/概念快捷键
  +池外票查询(chart+自由聊组合,预期管理提示)。

**改造五个文件:**
- reviewer.py(959→1076):generate_lesson(复用个股上下文+并发锁+usage记账)+LESSON_SYSTEM_PROMPT
  (数字禁编造/黑话禁出现)+_lesson_humanize(课文黑话代码级消毒——真实验收发现 prompt 拦不住
  AI 引用注入原文,改为决定性词典替换)+_lesson_number_check(编造数字抽查→low_confidence)。
- service.py(1247→1289):GET /api/brief(空池日报不再复活旧卡)/POST /api/brief/act
  (bought/sold/sold_half/research_yes/research_no;sold_half 当日幂等;bought 把日报卡的
  止损价写进池行,entry_B 不再漂移)/GET /api/lesson/{code}(当日缓存+refresh)/POST /api/push_quiet;
  路由:「/」→今日助理、/pro→旧专业看板、/ask→问AI。
- storage.py:lessons 表(UNIQUE(code,d)+KEEP_DAYS清理)+pool_update_amt(只许改仓位金额一列)。
- eod.py:_check_positions_mid 导出 held_peak/eff_stop(与引擎E1同构算式,第2包审查逐行对照)
  +流水线末端接 push.notify_daily(失败只告警)。
- static/settings.html:微信推送状态行+无事日推送开关。

**测试:test_web.py 180→277 用例(+97 个 T7 系列)**,覆盖翻译层口径/状态对账/幂等/
黑话零残留/钥匙零泄漏/缓存/代次/推送口径。

## 测试输出

```
test_rules.py      全部通过 ✓✓✓  (8/8)
test_rules_mid.py  全部通过 ✓✓✓  (16/16)
test_web.py        全部通过 ✓✓✓  (277/277)
```
screener.py / rules_mid.py / test_rules.py / test_rules_mid.py 全程零改动(红线)。

## 质量流程战果(为什么这次可以放心用)

- **逐包审查拦下的**:跨包按钮契约漏签(trimmed/reviewed 后端不识别)、无日报时持仓列表消失、
  池外查询降级成 toast 等——都在合入前修复。
- **最终整体审查(107代理)拦下的 15 个"逐包全绿但整体有缝"缺陷**,代表性的:
  ①sold_half 连点连续减半(critical,已加当日幂等+前端在途禁用);②新机会卡在真实流水线中
  是死代码,用户无法从首页立项(已改为与池现状对账);③已执行动作的卡片会复活(同根已修);
  ④entry_B 买入卡承诺的止损与实际记录不一致(已修);⑤黑话 condition 直通用户(已加
  condition_cn 词典层);⑥推送件数与首页口径漂移(已统一);⑦锁盈线比例写死不随参数(已改现读)。
- **真实浏览器验收**:今日页用真实日报渲染复核卡(黑话零残留);问AI页真实调 gpt-5.5 生成
  002463 三段课文+真实K线;PUSH_QUIET 开关闭环;全站 8 路由 200、控制台零告警。

## 文件行数对照

```
brief.py                   0 → 472 行   新增(白话翻译层)
push.py                    0 → 123 行   新增(微信推送)
static/home.html           0 → 401 行   新增(今日页,新首页)
static/ask.html            0 → 523 行   新增(问AI页)
reviewer.py              959 → 1076行   +课文层与黑话消毒
service.py              1247 → 1289行   +brief/act/lesson/push_quiet/路由
storage.py               829 → 879 行   +pool_update_amt/lessons
eod.py                   728 → 744 行   +展示字段导出/推送接线
static/settings.html     466 → 493 行   +推送状态与开关
test_web.py             2284 → 3329行   +97个T7用例
--- 未动 ---
screener.py / rules_mid.py / data.py / daily.py(注:任务7未改) / test_rules.py / test_rules_mid.py
static/index.html(md5零改动,原样挂 /pro) / 其余旧页面
```

## 偏离与备忘

1. 设计稿承诺项全部落地;「明确不做」两项(券商直连/截图记账)确未做。
2. 微信推送钥匙未配置(用户侧动作):在 .env 填 SERVERCHAN_SENDKEY(Server酱)或
   WECOM_WEBHOOK_URL 即通,设置页可见状态。
3. 遗留 Minor(不影响使用,记录在案):_latest_daily_report 与 build_stock_context 的
   同款循环重复实现(建议下沉 storage);ask 页 K线未带日/周切换(产品裁剪);
   renderOutpool 未清 CUR_CODE(死状态)。
4. 用户另开的两个后台小修会话(trim按钮样式/REMOVE映射)与主线的关系:后者主线已修
   (会话应空跑);前者若落盘,以三测为准合并。

## 验收操作(产品负责人按这 6 条验,对应设计稿验收标准)

1. 打开 http://127.0.0.1:8787 —— 10 秒内知道今天要不要动手(当前:1 件复核事项);
2. 等出现买入卡时核对:价格区间/金额股数/止损线/最多亏2% 四要素齐全,「为什么选它?」能展开三段课;
3. 全站找不到黑话;不懂的地方都有问AI入口;
4. 无事日打开是「今天无操作」大字;
5. 点「已买入/已卖出」自动记账(连点有防护),卡片处理后消失;
6. 齿轮里旧版页面全在(/pro /pool /daily /trade /settings /history),功能未少。

## 追加交付:任务7.5 全自动研究流水线(2026-07-07,用户拍板"别让我做选择")

- **规则修订(产品负责人口头定稿)**:研究与入池全自动,人工动作只剩两个——照买入卡下单、
  照卖出卡下单(工具不碰钱包,永不自动交易)。
- **实现**:候选自动立项(eod._auto_research,每轮5只成本护栏,越接近过期越优先 T7X8)→
  AI 研究报告 → reviewer.auto_pool_decision:评分≥AUTO_POOL_MIN_SCORE(params 注册,默认18,
  设置页可调)自动入「等买点」,低分自动放弃留 note,池满/解析失败留 research;
  冷启动 eod.bootstrap_candidates + POST /api/pool/bootstrap(从跟踪中票选质量前12,
  只看不追>25%过滤);首页 candidate/research 卡改通报型(唯一按钮「不感兴趣,放弃」)。
- **评分机制修复(冷启动实测暴露)**:首轮5只全被拒(8~14/35),根因=七因子6项"证据不足"
  ——数据供给喂不饱评分体系。修:①data.get_stock_profile 个股概况注入立项上下文(行业锚);
  ②检索纪律 v2——定性判断允许 AI 自有常识但必须标注「AI知识(截至训练时点)」,
  一切数字仍只认注入数据(防编造≠装失忆)。修后实测:评分恢复 16~20,阈值 18 有区分度。
- **真实运行结果(2026-07-07)**:等买点名单 5 只(沪电股份/华海清科20分/中国动力20/
  厦门钨业20/新易盛18);华勤技术16分自动放弃;剩余 7 只候选(含复活重测的中芯国际等)
  次日流水线自动接手。
- 三测终态:**8/8、16/16、299/299**(⑰节 T7X 系列 30+ 用例;审查 Important——配额排序
  退化已修并 T7X8 钉死)。

## 任务8 候选(用户已拍板顺序)

截图识别记账(AI视觉) > 交易台手录仓入口弱化 > ai_usage 按 token 估费。
