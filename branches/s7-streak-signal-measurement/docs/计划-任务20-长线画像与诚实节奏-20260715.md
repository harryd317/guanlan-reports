# 计划 · 任务20 长线画像标记 + 诚实节奏卡 + 证据链卡

日期:2026-07-15 · 原型:docs/原型-长线画像与诚实节奏-20260715.html(用户拍板:"主要还是你判断,我只是来拍板做选择"——方向已批,细节授权)
**性质:全部纯展示,零闸门变更,零冻结参数接触,快照 payload 零新键(golden master 铁律)。**

## 0. 铁律(违者返工)

1. run_eod_scan 与快照 payload 零改动——画像计算在 service/brief 层按需+日缓存,绝不进扫描流水线;
2. 画像**只标注不拦截**:买入卡出现与否、闸门行为与画像无关(测试钉死);
3. 判定引擎零接触;时间一律 Asia/Shanghai;四测只加不改(新节 TPF-A/B/C);
4. 文档路由白名单硬编码,不接受任意路径输入;
5. 前端禁词(更好/优于/胜出/已触发/已确认)不出现在新增 DOM 字符串;
6. 改 .py 后主会话统一 restart.sh;子代理不碰生产实例。

## 1. T-A 长线画像(profile.py 新模块 + 三处前端)

**画像三项**(每项 ✓/✗/?,?=数据缺失不计入分子,分母恒3):
- 首捕:code ∉ 冻结重放CSV(reports/backtest_mid_trades_v5params.csv,模块加载时读一次缓存集合)且 pool 表无更早行(min(created/first_seen) 即本轮)。口径注明"以2021-02以来重放史+池史为限";
- 贴平台:(当日收盘 − platform)/platform ≤ 5%(platform 取 pool 行,缺失=?);
- 动量带:120交易日累计涨幅(get_hist2 日缓存,pct复利)∈ [40%, 90%],不足120根=?。

**实现**:`profile.py` 纯函数 `long_profile(code, platform, close, fetch=None) -> {"first_capture","near_platform","momentum_band","score","denom","facts":[...]}`;`service` 层日缓存(kv `profile_{code}_{date}` 或内存 dict+日期守卫,选实现简单者);API:pool 等买点视图行与 `/api/brief` 买入卡 facts 附加 `long_profile` 键(**只加键**,旧断言零影响)。
**前端三处**:①pool.html 等买点视图行尾 tag「长线画像 n/3」(title=三项明细);②home.html 等买点速览行同款小 tag;③买入卡 facts 区一格 + 展开三行明细(照原型,含判例出处一行)。悬停明细文案照原型(首捕/止损便宜/不在抛物线末端)。

## 2. T-B 诚实节奏卡 + 证据链卡(纯前端+一个只读路由)

- trade.html 统计区下方常驻「这台机器的真实节奏」块(照原型:65% / 5~8笔 / 前5% / 20笔口径 / 三前提);数字为459笔回测口径的写死文案(来源注释指向 docs);
- strategy.html 影子台账卡下方「📚 为什么是这些规则」卡:五条链接(四路分析/重放一/重放二/36格协议/新易盛复盘+十例)→ `GET /doc/{key}`;
- service 新增 `/doc/{key}` 只读路由:硬编码白名单 key→docs 文件;.html 直接回(研究-长线方案三年重放-20260715.html),.md 用极简壳(escape 后 white-space:pre-wrap,统一导航不挂,顶部一行返回链接)。404=人话。

## 3. 测试(TPF 节,只加)

- TPF-A:profile 纯函数三项判定(首捕/CSV命中/池史更早行;贴平台边界5.0%;动量带39.9/40/90/90.1;数据缺失=?;score/denom);
- TPF-B:API 附加键形状 + **画像不拦截断言**(0/3 画像的票买入卡照常出)+ 旧断言回归;
- TPF-C:/doc 白名单(合法key回200含内容;非法key 404;路径穿越拒绝)+ 三页前端哨兵字符串(tag/节奏卡/证据链卡)+ 禁词零命中。

## 4. 验收

四测全绿 + golden master 双模式零差异(TPF-B 顺带断言快照无新键)+ restart.sh + curl(/doc 两个key + brief 含 long_profile)+ 浏览器 E2E(pool tag / trade 节奏卡 / strategy 证据链卡)。交付总结 + CLAUDE.md 补记 + 提交推送。
