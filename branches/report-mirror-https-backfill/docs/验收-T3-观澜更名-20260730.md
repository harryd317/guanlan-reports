# T3 观澜更名验收报告

## 一、交付状态

- 任务：T3 品牌更名“观澜”
- 分支：`codex/t3-guanlan-branding`
- 基线：`ea72736d3ae21b7211244e9da630043655089872`
- 工作树：`screener/.worktrees/t3-guanlan-branding`
- 状态：分支内实现与验收完成，等待用户评审
- 明确未做：未合并、未推送、未部署、未重启或切换生产、未开始 T4

## 二、实现清单

### 新增

- `branding.py`：唯一品牌配置及页面标题、通知签名、浏览器注入派生函数
- `static/about.html`：只读关于页，品牌文字由运行时注入
- `test_branding.py`：品牌、页面、关于页和三类通知边界测试
- `docs/superpowers/specs/2026-07-30-t3-guanlan-branding-design.md`
- `docs/superpowers/plans/2026-07-30-t3-guanlan-branding.md`
- 本报告及两张浏览器验收截图

### 修改

- `service.py`：OpenAPI 可见标题、`/brand.js`、`/about`
- `push.py`：Server酱/企业微信统一出站标题签名
- `eod.py`：macOS 原生通知统一出站标题签名
- `static/nav.js`：导航从运行时品牌配置读取名称
- 九个既有页面：加载 `/brand.js`，页面标题改为 section 输入
- `static/home.html`：侧栏品牌位改为运行时占位
- `static/settings.html`：增加关于页入口
- `static/stock_detail.html`：个股名称到达后重做品牌标题
- `test_web.py`：两处旧通知标题期望更新为 T3 签名

## 三、品牌单一来源

唯一产品文案常量位于 `branding.py`：

- 主文案：观澜
- 副文案：A股纪律投研台
- 品牌句：观水有术，必观其澜

排除测试与文档后，全仓搜索三条完整文案，只有 `branding.py` 命中。前端通过 `/brand.js` 消费同一份配置，没有第二份 HTML/JavaScript 品牌常量。

旧品牌搜索结果：

- `static/`、`service.py`、`push.py`、`eod.py` 中完整搜索 `Screener|纪律选股`：零命中。
- `test_branding.py` 中保留两条否定断言，专门防止旧品牌重新进入可见页面。
- 仓库内其他小写 `screener` 代码标识符、文件名、数据库名、环境变量和仓库名均按禁改纪律保留。

## 四、自动化验证

### T3 新增测试

- `python -m unittest -q test_branding`
- 结果：13/13 通过
- 覆盖：
  - 页面标题派生；
  - 通知签名只加一次；
  - 浏览器品牌注入；
  - 九个既有页面；
  - 首页与导航品牌位；
  - 个股动态标题；
  - 关于页路由、内容与设置入口；
  - Server酱、企业微信、macOS 最终出站标题。

通知测试保持真实 `send_push` / `_macos_notify` 逻辑，只把外部 HTTP 和 `osascript` 执行器替换为捕获器。最终出站证据为：

- Server酱：`{"title":"[观澜] 日报已生成","desp":"今日无操作"}`
- 企业微信：首行 `[观澜] 日报已生成`
- macOS：`with title "[观澜] 日报已生成"`

测试未向真实微信或系统通知发送消息。

### 既有回归

- 短线规则：8/8 通过
- 中线规则：20/20 通过
- 市况分类：9/9 通过
- M3 首页与镜像同步辅助测试：33/33 通过
- T1/S3 数据与界面：51/51 通过
- 全站回归：834/834 通过
- Python 编译：通过
- `git diff --check`：通过

全站回归第一次按旧预期得到 832/834；仅两条失败对应 Server酱和企业微信仍期待未签名标题。把这两条验收期望更新为冻结的 T3 签名后，完整重跑为 834/834。

## 五、浏览器验收

使用隔离端口 `127.0.0.1:8878`、临时 `screener.db`、临时研究库和临时日志目录运行；没有占用或重启生产端口。

### 桌面首页

- 视口：1440 × 900
- `document.title`：`观澜 · 今日`
- 侧栏品牌：`观澜`
- 可见旧品牌：否
- 截图：`docs/证据-T3-观澜-首页-桌面-20260730.png`
- SHA-256：`2e921b3b2a40f2f60ef161e4c115bd7ca63255a7451b10bc94c9ce3412bf4640`

### 手机关于页

- 视口：390 × 844
- `document.title`：`观澜 · 关于`
- 主文案、副文案、品牌句：三项逐字匹配
- 横向溢出：否
- 浏览器控制台错误：0
- 截图：`docs/证据-T3-观澜-关于-手机-20260730.png`
- SHA-256：`06d76a83d465a6914c00fef4b94fc631c4cbb89601100c43f0a1596d089cfd64`

隔离浏览器验收过程中，生产 `market.db` 目标文件修改时间仍为 `2026-07-29 21:59:09`，未发生写入。

## 六、禁区与依赖审计

- [x] 未改策略规则、策略插件或生产 S2 行为
- [x] 未改风控逻辑
- [x] 未改回测核心
- [x] 未改行情/研究数据管道
- [x] 未改数据库结构、字段、索引或生产数据
- [x] 未改内部代码标识符、文件名、数据库名、API 旧路径、环境变量或仓库名
- [x] `eod.py` 只改 `_macos_notify` 的可见标题边界
- [x] `push.py` 只改统一出站标题边界
- [x] `requirements.txt` 零差异
- [x] 零新增依赖
- [x] 无演示数据和硬编码股票
- [x] S3 v0.1/v0.2/v0.3、S5 制品与分支未改
- [x] T4 未动工

## 七、T3 验收自查

- [x] 网页标题统一显示“观澜”
- [x] 首页侧栏与统一导航显示“观澜”
- [x] 主文案、副文案、品牌句收口到一个配置常量
- [x] 关于页可由设置页进入
- [x] Server酱标题带 `[观澜]`
- [x] 企业微信标题带 `[观澜]`
- [x] macOS 通知标题带 `[观澜]`
- [x] 重复传入已签名标题不会二次签名
- [x] 用户可见旧品牌无残留
- [x] 桌面与手机真实浏览器验收通过
- [x] 全量自动化回归通过
- [x] 分支未合并、未部署、生产未切换

结论：T3 分支具备截图评审条件。只有用户明确验收通过并授权合并后，才能合并 `main`；T4 在此之前保持未动工。
