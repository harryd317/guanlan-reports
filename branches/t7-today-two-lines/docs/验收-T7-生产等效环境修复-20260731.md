# T7 生产等效环境修复验收报告

- 日期：2026-07-31
- 分支：`codex/t7-today-two-lines`
- 验收环境：生产 LaunchAgent 克隆副本，端口 `18787`
- 结论：五页桌面与手机验收通过
- 部署状态：未重新部署
- 任务状态：未完成；等待镜像远端验证与用户复核

## 验收结论

生产等效实例使用与生产相同的启动脚本、应用入口和静态文件服务方式。它从 T7 工作树启动，读取三套只读 SQLite 一致性副本，不运行初始化、迁移、调度或写接口。

根因不是页面模板或响应式 CSS。首次生产验收使用 `tab.screenshot({fullPage:true})`；该模式在显式视口覆盖下稳定生成空白或错误重排的图片。普通视口截图与页面 DOM 始终正常。本轮已禁止 `fullPage` 进入部署验收证据，并增加图像格式、像素、DOM 宽度、横向溢出和关键内容矩形守卫。

详细排查见 `docs/事件记录-T7部署回滚-20260731.md`。

## 五页十图

截图目录：

```text
docs/screenshots/t7-production-equivalent/20260731/
```

| 页面 | 桌面证据 | 手机证据 | 结果 |
| --- | --- | --- | --- |
| 今天 | `今天-桌面.jpg` | `今天-手机.jpg` | 通过 |
| 股票池 | `股票池-桌面.jpg` | `股票池-手机.jpg` | 通过 |
| 我的 | `我的-桌面.jpg` | `我的-手机.jpg` | 通过 |
| 工程模式 | `工程模式-桌面.jpg` | `工程模式-手机.jpg` | 通过 |
| 设置 | `设置-桌面.jpg` | `设置-手机.jpg` | 通过 |

桌面证据均为 `1440×900`；手机证据均为 `390×844`。十张图片均为真实 JPEG 字节，扩展名为 `.jpg`。逐图 DOM 探针、像素尺寸与 SHA-256 位于：

```text
reports/t7-production-equivalent-20260731/visual-evidence.json
```

## 哈希与隔离证明

生产 plist、三套生产数据库和三套只读副本的启动前后 SHA-256 全部一致。清单位于：

```text
reports/t7-production-equivalent-20260731/before-hashes.json
reports/t7-production-equivalent-20260731/after-hashes.json
```

关键证据：

- 生产 plist：`93e862839f8a54ee25d2aefc704742eb82ba106cd7b5239342b5245fc2a42dac`
- 生产 `screener.db`：`01506181ce74155a76a3eab3085c8b7198563ea47555e8f5f1cce59cb534ea08`
- 生产 `market.db`：`cbd061c495ef334afdda1de79249e929c4ba056bb4198f48942869585e781cad`
- 生产 `S3_research.db`：`882562e8d0ee16517b7d5308770fc390b1310dbdc256edcbb79cd91a909d673e`

生产服务在验收前后均为 PID `3802`。克隆实例已卸载，18787 已停止监听；生产 8787 继续健康运行。

## 测试

- `test_rules.py`：`8/8`
- `test_rules_mid.py`：`20/20`
- `test_web.py`：`838/838`
- `test_regime.py`：`6/6`
- `test_production_equivalent.py`：`20/20`

截图守卫先出现预期 RED，再转为 GREEN。相关源提交：

- `73ed1723e4c9db7617421d26613ec7f60f6517cb`
- `b7e53fe`

## 重新部署门禁

本报告只证明 T7 已通过生产等效环境验收，不授权生产部署。下一步必须：

1. 用 HTTPS 把本报告、事件记录、十张截图、哈希清单和 T7 全部 `docs/`、`reports/` 同步到公开镜像；
2. 从镜像远端重新读取并验证文件和提交；
3. 回报镜像提交哈希；
4. 等待用户在镜像复核十张截图并明确批准。

当前 HTTPS 推送因本机 GitHub 凭据属于 `NONameToo` 而被目标仓库拒绝。没有新的镜像提交哈希，因此 T7 仍为未完成、未部署状态。
