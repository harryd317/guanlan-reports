# 任务21 · 全站重设计四期(低频页换肤)+ /pro 退役

> **执行方式:** 子代理TDD+双审+fable终审,同前三期。
> **用户拍板(2026-07-15):** "接着把四期也做了,pro也可以退役了"——这是交付总结17/18/19 三次留档等待的授权:**允许退休 /pro 及其配套旧锁(T5F1/_src_idx/T7E3/T10B1/T10B2),对这些用例的修改/退役不受"既有用例只加不改"约束**(其余用例照旧只加不改)。
> **视觉依据:** home/pool/ask/trade 活样板(三期成熟三件套:`:not(.unav)` 收窄 + style段外SHA1哨兵 + 三断言模板)。

**目标:** ①daily/history/settings/stock_detail 四页换肤(纯样式,铁则同三期) ②/pro 退役:路由重定向 /pool、index.html 删除、全站死DOM(隐藏topnav/gearpanel)与旧锁清理。

## 全局约束(同前三期,另加)

- 判定链四文件+strategies/*零改动;**/api/dashboard 绝对不许动**(pool 视图③依赖它,退役的只是页面不是接口)
- 换肤铁则(20-1):JS/文案/DOM 零动,diff 封闭在 <style> 内;每页做 `header:not(.unav)` 收窄
- 旧锁修改白名单(20-2 仅限):T5F1(1998附近)/_src_idx 块(2002附近的全部消费断言)/T7E3(2916)/T10B1(4044)/T10B2(4064)/T17A4(6189 九页清单去 index.html);改动方式=改写为"新真相"断言(不是删空),每处留注释「2026-07-15 用户拍板 /pro 退役,旧锁改写」
- 白名单之外的既有用例仍然只加不改;E2E 禁改库;手术式提交;并行会话防护同前

---

### 任务21-1: 四页换肤(daily/history/settings/stock_detail)

**Files:** Modify 四个 static/*.html(仅样式层)+ static/ask.html(仅一行:@media(max-width:680px){.toast{bottom:90px}},三期遗留#2 顺手);Test test_web.py ㉚节 T21A×4(每页一条:新令牌特征值+结构锚代表+JS哨兵合一)
**要点:** :root 照 pool 逐字节;组件类对齐;每页裸 header{} 改 `header:not(.unav)`(三期遗留[置顶]);死CSS顺手清;**四页逐页做、每页独立跑 test_web**,全部完成一次 commit `任务21-1: 四期低频页换肤`
**自证:** 每页 style 段外剥离 SHA1 前后一致(报告附剥离脚本命令行——三期流程改进落地)

### 任务21-2: /pro 退役 + 旧锁退休 + 死DOM清理

**Files:** service.py(/pro 路由改 RedirectResponse("/pool",307);删 page_pro 的 FileResponse);Delete static/index.html;Modify static/home.html(维修间卡去「专业看板」链接;**gearpanel 死DOM+saveAcct 死代码删除**)、static/settings.html(维修间去「旧版专业看板」链接)、static/pool.html+daily+trade+settings+history+stock_detail(各自隐藏 `<nav class="topnav">` 死DOM删除);Test test_web.py(白名单锁改写+T21B×4 新锁)
**旧锁改写方案:**
- T5F1 → 改写断言:全站静态页**不再含** `<nav class="topnav">`(死DOM清尽)
- _src_idx 消费断言(先 Read 1984-2030 现场逐条核对)→ 随 index.html 删除一并改写/退役,留注释
- T7E3「旧看板未被改动」→ 改写:`/pro` 路由源码含 RedirectResponse 且 static/index.html 不存在
- T10B1/B2 → 改写:home.html 不再含 gearpanel/saveAcct/acct-unit;账户编辑入口=交易台(trade.html 含「改」按钮断言)
- T17A4 → 九页清单去掉 index.html(注释说明)
- 新增 T21B×4:①/pro 302/307 重定向到 /pool(直调路由函数或 TestClient)②index.html 文件不存在③全站无 href="/pro" ④home 无 gearpanel 且 trade 有账户编辑
**步骤:** Read 现场锁块→落 T21B(RED)→逐项施工(service.py 先行+restart.sh→删文件→六页死DOM→home 齿轮遗产→两处链接→锁改写)→四测全绿→commit `任务21-2: pro退役+旧锁退休(用户拍板20260715)`
**风险闸:** 删 index.html 前 `git ls-files` 确认它在版本库(可随时找回);ai_drawer 共享资产不动(/pro 内联版随文件消亡,z-index 同病自动销账)

### 任务21-3: E2E 验收 + 交付

- 四测终跑+restart 指纹;E2E:四页新皮渲染(桌面+375px)/`/pro` 重定向落 /pool/全站导航高亮/console 零错误;截图
- CLAUDE.md 任务21段;docs/交付总结-20-四期与pro退役-20260715.md(遗留清单收敛);commit+push
