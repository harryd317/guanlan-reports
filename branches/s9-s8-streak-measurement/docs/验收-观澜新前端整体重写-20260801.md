# 观澜新前端整体重写验收报告

- 日期：2026-08-01
- 结果：生产部署通过
- 部署合并提交：`b2aed798a654926c32180820fbe998abd99c3600`
- 生产进程：LaunchAgent `com.rui.stock-screener.web`，PID `71357`
- 健康指纹：启动与磁盘均为 `9b2db1af10049e486be89fb6e79c5e607e5ec424`

## 结论

旧前端已整体退役，新前端以确认原型为唯一界面本体。生产现在只有“今天 / 股票池 / 我的”三个导航入口，并提供股票池内的个股详情界面；旧页面 URL 使用默认 404，不显示迁移提示。

新界面已接入现有真实 API。账户权益仍只通过既有 `POST /api/account` 保存；没有新增写接口，也没有修改策略、风控、数据管道、记账或通知逻辑。`service.py` 的 68 个 `/api/*` 函数源码逐字哈希全部不变。

## 交付范围

- 今天：投机线开仓门与市况、投资线建仓进度、待处理、等买点、市场广度。
- 股票池：真实名单表格、选股逻辑子页、点击真实股票进入详情。
- 个股详情：价格阶梯、关键数字、状态时间线、一句依据、返回股票池。
- 我的：账户权益内联编辑、投机仓与投资仓、实盘验证期、建仓常量。
- 建仓常量：每周三、5 批、每批 1.4 万元；沪深300单日跌超 2% 时当周加倍。
- 旧前端归档：`archive/frontend-legacy-20260801/static/`，文件级清单为其中的 `SHA256SUMS`。

真实空值按空值展示，不编造策略数据：当前 S3 广度汇总为空，因此 NH / NL 显示 `—`；验收股票 002156 的后端止损为空，因此止损显示 `—`。

## 不可变边界

| 检查项 | 结果 |
| --- | --- |
| `/api/*` 函数源码 | 68 / 68 未变 |
| 新增前端写路径 | 无 |
| 权益保存路径 | 仅既有 `POST /api/account` |
| 策略 / 风控 / 数据 / 记账 / 通知 | 未修改 |
| 旧静态页面路由 | 已删除，默认 404 |

API 前后源码清单：

- `reports/guanlan-frontend-20260801/baseline/api-source-manifest.json`
- `reports/guanlan-frontend-20260801/after/api-source-manifest.json`
- `reports/guanlan-frontend-20260801/verification/api-source-manifest.json`

## 生产等效验收

生产等效环境按生产 LaunchAgent 的工作目录、启动脚本和环境变量克隆，使用只读 SQLite 一致性快照。`launchctl bootstrap` 在隔离实例上返回 EIO，因此使用相同克隆配置直接运行同一 `start.sh`；这只影响临时实例的启动器，不影响应用验收或生产部署。

| 界面 | 1440×900 | 390×844 |
| --- | --- | --- |
| 今天 | 一屏、无滚动、无控制台错误 | 一屏、无滚动、无控制台错误 |
| 股票池 | 一屏、无滚动、无控制台错误 | 一屏、无滚动、无控制台错误 |
| 个股详情 | 一屏、无滚动、无控制台错误 | 一屏、无滚动、无控制台错误 |
| 我的 | 一屏、无滚动、无控制台错误 | 一屏、无滚动、无控制台错误 |

- 八张证据均为普通 viewport 截图，图像尺寸与视口精确一致。
- 股票池真实行 `002156` → 详情 `#stock/002156` → 返回股票池：通过。
- 临时可写副本中的权益 `503000` → 保存 `504000` → 刷新仍为 `504000`：通过。
- 生产数据库未参与权益写入测试。

完整矩阵：`reports/guanlan-frontend-20260801/acceptance/acceptance-matrix.json`

截图：

- `reports/guanlan-frontend-20260801/acceptance/screenshots/today-desktop.jpg`
- `reports/guanlan-frontend-20260801/acceptance/screenshots/pool-desktop.jpg`
- `reports/guanlan-frontend-20260801/acceptance/screenshots/stock-desktop.jpg`
- `reports/guanlan-frontend-20260801/acceptance/screenshots/me-desktop.jpg`
- `reports/guanlan-frontend-20260801/acceptance/screenshots/today-mobile.jpg`
- `reports/guanlan-frontend-20260801/acceptance/screenshots/pool-mobile.jpg`
- `reports/guanlan-frontend-20260801/acceptance/screenshots/stock-mobile.jpg`
- `reports/guanlan-frontend-20260801/acceptance/screenshots/me-mobile.jpg`

## 自动化验证

- 前端与交付聚焦测试：107 / 107 通过。
- 仓库规定回归：`test_rules.py` 8 / 8、`test_rules_mid.py` 20 / 20、`test_web.py` 838 / 838、`test_regime.py` 6 / 6，共 872 / 872 通过。
- 生产探针：`/`、`/api/health`、`/api/pool` 返回 200。
- 旧路由：`/pro`、`/daily`、`/stock/600000`、`/page/pool` 返回 404。
- 生产浏览器复核：桌面和手机四界面一屏无滚动，股票池进入详情并返回通过。

详细测试记录：`reports/guanlan-frontend-20260801/verification/test-results.txt`

## 数据库哈希说明

首次标准重启时，旧进程把既有 WAL 状态 checkpoint 到主文件，导致 `screener.db` 原始 SHA-256 从 `d3c201…` 变为 `ecfb06…`。数据库含 schema 的逻辑 SHA3 在变化前后均为 `493d8ccb7d4b1c04d322992c891ae42e5eab0db609b8dc95feeaef81`，证明业务数据没有变化；另外两库和 LaunchAgent 原始哈希也没有变化。

随后以 checkpoint 已稳定的文件作为部署前基线，再次执行同一 `restart.sh`。最终部署尝试的原始哈希前后一致：

| 文件 | 部署前 SHA-256 | 部署后 SHA-256 |
| --- | --- | --- |
| `screener.db` | `ecfb06f626a5003518c335dbbb919469bb700e6398d34a68540e3a74e5f2f370` | `ecfb06f626a5003518c335dbbb919469bb700e6398d34a68540e3a74e5f2f370` |
| `market.db` | `a504f873eb204bee722082b1d6fec92b04e950cbae16842d4c4917a820c75fc1` | `a504f873eb204bee722082b1d6fec92b04e950cbae16842d4c4917a820c75fc1` |
| `S3_research.db` | `bf7aef744b05a69a16416c32f10663865a798f77ad828f93de54e6421018fb7b` | `bf7aef744b05a69a16416c32f10663865a798f77ad828f93de54e6421018fb7b` |
| LaunchAgent plist | `93e862839f8a54ee25d2aefc704742eb82ba106cd7b5239342b5245fc2a42dac` | `93e862839f8a54ee25d2aefc704742eb82ba106cd7b5239342b5245fc2a42dac` |

部署证据：`reports/guanlan-frontend-20260801/deployment/deployment.json`

工作树中用户原有未跟踪文件 `docs/AI评分校准-202608.md` 未纳入本任务提交。
