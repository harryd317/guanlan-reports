# 产业温度计与产业档案页验收报告

- 日期：2026-08-02
- 交付分支：`codex/industry-thermometer-archives`
- 分支收尾提交：`1e1176bd98dda57cb1c786dfbafa28570508163b`
- main 合并提交：`00e3ef636f9bb813756d250178c23dc8f3c16e19`
- 边界：纯展示，零交易；未改策略、风控、选股、交易和收盘任务
- 状态：随附原文、S3 精确清理、生产等效验收、main 合并和生产重启验证均已闭环

## 一、交付内容

1. 新增申万二级行业四灯温度计，合成冷、温、热、烫四档，缺数单独显示“待更新”。
2. 股票池新增第三子页签“产业档案”，支持行业药丸过滤、左侧列表、右侧 Markdown 阅读和深链。
3. 档案每次请求扫描 `docs/产业档案/*.md`，新提交文件无需登记。YAML 头支持单行、逗号、内联列表和多行列表。
4. Markdown 以 CommonMark+表格渲染，原始 HTML 禁用，请求 ID 只能反查已扫描目录，阻断路径穿越。
5. 个股在计算日的申万二级行业与档案 `industry` 相交时显示档案入口，无匹配则整行隐藏。
6. 只新增三个 GET API：`/api/industry-thermometer`、`/api/industry-archives`、`/api/industry-archives/{archive_id}`。

## 二、计算口径与现场修正

真实 T1 全量库首跑时，“最新共同日”测试失败。调试确认：

- `cluster_daily` 与 `daily_vol` 覆盖 2015-01-05 至 2025-12-31；
- T1 中 `warm_daily`/`warm_adj_factor` 只覆盖 2013-12-02 至 2014-12-31；
- 既有 `market.db` 的 `market_daily`/`adj_factors` 覆盖 2015-01-05 至 2026-07-31。

因此补了独立行情库失败测试，然后做最小修正：集群灯、量比灯和历史成员继续只读 T1；成交占比与 120 日 RS 只读复用既有 `market.db`。计算日取两库四表共有的最新交易日。未新增数据源、定时任务、表或写路径。

全量结果：

| 项目 | 结果 |
| --- | ---: |
| 数据日 | 2025-12-31 |
| 行业总数 | 134 |
| 四项完整行业 | 131 |
| RS 有效股票 | 5,409 |
| 冷 / 温 / 热 / 烫 / 缺数 | 20 / 83 / 23 / 5 / 3 |

## 三、生产等效环境

- 生产 LaunchAgent 原文件一字未改，克隆标签为 `com.rui.stock-screener.web.industry-archives-equivalent`。
- 克隆工作目录为本分支工作树，启动脚本仍为同一 `start.sh`，端口 `19787`。
- `screener.db`、`market.db`、`S3_research.db` 使用 SQLite 一致性只读副本；完整 T1 库以 SQLite `mode=ro` + `query_only=ON` 打开。
- `/api/health` 返回 `ok=true`、`match=true`，启动/磁盘指纹都为 `9b2db1af10049e486be89fb6e79c5e607e5ec424`。
- 向 `/api/account` 发送写探针返回 403。
- 验收结束后已卸载克隆 LaunchAgent，`19787` 无监听。

等效验收启停窗口内，所有哈希完全相等：

| 对象 | SHA-256 |
| --- | --- |
| 生产 plist | `93e862839f8a54ee25d2aefc704742eb82ba106cd7b5239342b5245fc2a42dac` |
| 生产 screener | `ecfb06f626a5003518c335dbbb919469bb700e6398d34a68540e3a74e5f2f370` |
| 生产 market | `a504f873eb204bee722082b1d6fec92b04e950cbae16842d4c4917a820c75fc1` |
| 生产 S3 | `bf7aef744b05a69a16416c32f10663865a798f77ad828f93de54e6421018fb7b` |
| 副本 screener | `94c99dbfaaeaa507362d95837cdd7ee2b9c7f403adfca45d690706582f5894ff` |
| 副本 market | `4ee29aa68fdc116bec07de56a1b258eab3437e87568aadfa1f2d84d46bd9ca1c` |
| 副本 S3 | `e38deb016eb48bdabcb5113d1f5198d8c8eb82fc0e4d7fd76ff883275bdaedd1` |
| 完整 T1 | `111f2017d476e87dfdc68d3541d7678630d466d410a91f4ab37d8bcb9ce049d5` |

随附原文补齐及 S3 精确清理后，再次按同一规则复验：

- 克隆标签：`com.rui.stock-screener.web.industry-archives-final`；端口 `19788`；工作目录仍为本分支工作树；启动脚本仍为 `start.sh`。
- `/api/health`：`ok=true`、`match=true`，启动/磁盘指纹均为 `9b2db1af10049e486be89fb6e79c5e607e5ec424`。
- `/api/account` 写探针返回 403；验收后已卸载，19788 无监听。
- 启停前后生产 plist、三份生产库及三份只读副本的 SHA-256 逐项一致：

| 对象 | 复验 SHA-256 |
| --- | --- |
| 生产 plist | `93e862839f8a54ee25d2aefc704742eb82ba106cd7b5239342b5245fc2a42dac` |
| 生产 screener | `ecfb06f626a5003518c335dbbb919469bb700e6398d34a68540e3a74e5f2f370` |
| 生产 market | `a504f873eb204bee722082b1d6fec92b04e950cbae16842d4c4917a820c75fc1` |
| 生产 S3 | `90312194780c8e443102cfe4bde9d1a69c6363f4300ef6f7182b816624ff3d54` |
| 副本 screener | `94c99dbfaaeaa507362d95837cdd7ee2b9c7f403adfca45d690706582f5894ff` |
| 副本 market | `4ee29aa68fdc116bec07de56a1b258eab3437e87568aadfa1f2d84d46bd9ca1c` |
| 副本 S3 | `5f292ef743043bd1c21a4de82e936c4c0b967e6db36692e511b01c8ef20236a3` |

## 四、双视口视觉与交互

| 验收项 | 桌面 1440×900 | 手机 390×844 |
| --- | --- | --- |
| 产业档案页 | 通过 | 通过 |
| 文档横向溢出 | 0 | 0 |
| 文档纵向溢出 | 0 | 0 |
| 134 个行业名 | 全部显示 | 全部显示 |
| Markdown 表格 | 6 张正常渲染 | 阅读区单列 |
| 控制台错误 | 0 | 0 |

补齐原文后的专项复验：桌面与手机均显示完整档案标题，解析出 3 张表、14 个标题；文档横向溢出均为 0，控制台错误均为 0。手机档案布局为单列 `366px`。

交互实测：

- 点“半导体”药丸后，列表从 2 篇过滤为唯一匹配档案。
- 通富微电 `002156` 显示“产业档案”链接，点击后深链到硬件链档案。
- 大族激光 `002008` 无匹配档案，链接行完全隐藏。

截图位于 `docs/screenshots/industry-archives-equivalent/20260802/`：

- `产业档案-桌面.jpg`
- `产业档案-手机.jpg`
- `产业档案-半导体过滤-桌面.jpg`
- `产业档案-表格阅读-桌面.jpg`
- `个股产业档案入口-桌面.jpg`
- `个股产业档案入口-手机.jpg`
- `产业档案-原文-等效-桌面.jpg`，SHA-256 `6b38b85ed7a6cf67ccc24bb51f9ffeab26226da56c9e2078bb38b695cbe11e65`
- `产业档案-原文-等效-手机.jpg`，SHA-256 `a8bb3dcde618a4c0b6883da84fc3155cb5e9363e7db6b515ec28b62d4e17309e`
- `产业档案-原文-生产-桌面.jpg`，SHA-256 `5ce3b1a1afeb7b4a845d9a4f17ab7a6676fcfa79f3b2e301365eeba72bdc0baf`
- `产业档案-原文-生产-手机.jpg`，SHA-256 `2f558b1c6f97c8d2e419d2cb2b7a658e6452f7e901d61792389e2d8be4bd7826`

截图尺寸和 SHA-256 见 `reports/industry-archives-equivalent-20260802.json`。

## 五、自动化验证

- 产业温度计、档案、API、新前端、原 API 基线和等效模式聚焦测试：54/54。
- `test_rules.py`：8/8。
- `test_rules_mid.py`：20/20。
- `test_web.py`：838/838。
- `test_regime.py`：6/6。
- `static/home.html` 两段 JavaScript 语法：2/2。
- `git diff --check`：无错误。

上述全套测试在功能分支通过后，又在 main 合并提交的全新临时工作树中逐项复跑并全部通过；测试库只写入临时工作树，生产 S3 哈希保持 `90312194780c8e443102cfe4bde9d1a69c6363f4300ef6f7182b816624ff3d54`。

独立工作树首跑回归曾因三份未被 Git 跟踪的冻结基线文件缺失失败。从主工作树复制后逐份 SHA-256 一致，代码无需为此修改。

## 六、随附原文与生产 S3 污染闭环

### 1. 随附原文

已用 `~/Downloads/产业档案-存储与算力硬件链.md` 逐字节替换空态档案，两文件 SHA-256 均为 `2cdbfbc6b0f0c2bb5c1c1c154ec307ee6e1dfbc972c371db41cc2e7120cfc50e`。

原文 YAML 使用 `industry: 半导体 · 存储与算力硬件`。为避免“半导体”过滤和个股匹配失效，先补稳定失败测试，再做最小兼容：中点分隔符解析为两个行业标签。专项测试完成红→绿，原文正文未修改。

### 2. 精确清理主工作树回归误写的 S3 拒绝审计记录

最终复核发现，生产 `S3_research.db` 在等效验收结束后由 `bf7aef744b05a69a16416c32f10663865a798f77ad828f93de54e6421018fb7b` 变为 `9752050d5071e1fcfa3bc51aa5147053bdff19f898f787fcc4fd4356dc2a0a34`。时间戳和逻辑转储差异确认，这是此前为诊断独立工作树基线缺失而在主工作树运行 `test_web.py` 时产生的测试污染：

- `job_runs` 新增 id=8；
- `job_name=s3_incremental`，`trade_date=2026-06-07`，状态 `rejected`；
- `started_at=finished_at=2026-08-01T17:59:46.726261+00:00`；
- 错误为 `breadth rows are empty`、`clusters rows are empty`；
- 除该行及 `sqlite_sequence(job_runs)` 从 7 变 8 外，两库逻辑转储逐行一致。

经用户明确授权，只对该记录和序列值执行带事务守卫的精确清理。任何字段、行数、最大 id 或序列值不符合预期，事务都会因 CHECK 约束失败而回滚。

| 项目 | 清理前 | 清理后 |
| --- | --- | --- |
| 生产 `S3_research.db` 全库 SHA-256 | `9752050d5071e1fcfa3bc51aa5147053bdff19f898f787fcc4fd4356dc2a0a34` | `90312194780c8e443102cfe4bde9d1a69c6363f4300ef6f7182b816624ff3d54` |
| `job_runs WHERE id=8` | 1 行 | 0 行 |
| `sqlite_sequence(job_runs)` | 8 | 7 |
| `job_runs` 行数 / 最大 id | 8 / 8 | 7 / 7 |
| `PRAGMA quick_check` | `ok` | `ok` |

清理前另用 SQLite backup API 留存一致性备份，备份 SHA-256 为 `33f91e20c2315c308cbfd81c3dbd8e53863376f10565029116cc50888aac59a2`。清理后的逻辑转储与污染前等效验收副本逐行一致，diff 为空；除删除 id=8 和把对应序列从 8 回退到 7 外，没有其他逻辑差异。

## 七、main 合并与生产部署

- `codex/industry-thermometer-archives` 以 `--no-ff` 合并 main，合并提交为 `00e3ef636f9bb813756d250178c23dc8f3c16e19`。
- 使用生产原 `restart.sh` 对既有 `com.rui.stock-screener.web` LaunchAgent 执行 `kickstart -k`；生产 plist 未修改。
- 重启后 `/api/health` 返回 `ok=true`、`match=true`，启动/磁盘指纹均为 `9b2db1af10049e486be89fb6e79c5e607e5ec424`，规则版本为 10。
- 生产档案 API 返回两篇档案；随附原文解析为 `半导体`、`存储与算力硬件` 两个行业标签，状态为“样板（格式验收用）”。
- 生产浏览器双视口复核：桌面和手机均显示完整标题、3 张表、14 个标题；文档宽度分别为 1440 和 390，无横向溢出，控制台错误均为 0；手机阅读布局为单列 `366px`。

部署前后证据完全一致：

| 对象 | 部署前 SHA-256 | 部署后 SHA-256 |
| --- | --- | --- |
| 生产 plist | `93e862839f8a54ee25d2aefc704742eb82ba106cd7b5239342b5245fc2a42dac` | `93e862839f8a54ee25d2aefc704742eb82ba106cd7b5239342b5245fc2a42dac` |
| 生产 screener | `ecfb06f626a5003518c335dbbb919469bb700e6398d34a68540e3a74e5f2f370` | `ecfb06f626a5003518c335dbbb919469bb700e6398d34a68540e3a74e5f2f370` |
| 生产 market | `a504f873eb204bee722082b1d6fec92b04e950cbae16842d4c4917a820c75fc1` | `a504f873eb204bee722082b1d6fec92b04e950cbae16842d4c4917a820c75fc1` |
| 生产 S3 | `90312194780c8e443102cfe4bde9d1a69c6363f4300ef6f7182b816624ff3d54` | `90312194780c8e443102cfe4bde9d1a69c6363f4300ef6f7182b816624ff3d54` |

部署后再次只读核对：`job_runs.id=8` 为 0 行，`sqlite_sequence(job_runs)=7`，`job_runs` 行数/最大 id 为 7/7，`PRAGMA quick_check=ok`。

## 八、镜像闭环

本报告、设计、计划、两篇档案、证据 JSON 和十张截图须提交 main，再通过 HTTPS 同步报告镜像并从全新克隆验证。最终可查镜像提交哈希在任务最终回报中给出。
