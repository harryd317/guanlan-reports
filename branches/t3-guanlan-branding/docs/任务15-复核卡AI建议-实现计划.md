# 任务15 · 复核卡直接给 AI 建议 — 实现计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task.

日期:2026-07-10 · 起因:用户反馈复核卡"要给个结论"却没给 AI 意见,"看过了"无从判断(与任务7.5"别让我做选择"一脉)。
拍板(原型 docs/原型-复核卡AI建议-20260710.html):①「照建议办」一键执行+「不同意」反向;②AI 建议限定 remove/keep 二选一;③覆盖所有带票复核卡,修数据类不给。

## Global Constraints

- 机械层事实文案与判定零改动;AI 建议只增不改(并排展示,终判在人——铁律9 口径);
- 成本护栏与事件解读同款:引擎不可用(None)停止后续调用、卡降级显示;引擎忙(busy)只跳单票;
- AI 输出必须解析为 remove/keep 枚举,解析失败该票降级无建议,不许瞎猜方向;
- 完工三测全绿 + kickstart 生产。

## Task 1: reviewer.review_verdict(先红后绿,T15A)

`reviewer.py` 新增(与 interpret_event 同构:_run_lock 并发1、_ask_engine、None=不可用/busy=跳过):
- 系统提示词:第一行只输出 remove|keep;第二行起两三句白话理由,引用注入数字,严禁编造;不许骑墙(保留要给条件);
- 输入 ctx:复核触发原因(condition)+ 技术状态(tech 摘要)+ 论点档案;
- 解析:首行前缀匹配枚举;失败全文关键词兜底(两词共存则放弃);why 截 300 字;log_ai_usage("review_verdict")。
- T15A:mock _ask_engine 返回 "remove\n理由…" → verdict/why;返回胡话 → busy 降级;抛 ReviewerError → None。

## Task 2: daily 挂建议(T15B)

`generate_daily_report(..., review_verdict=None)` 新参数;cards 组装后对 REVIEW 类带票卡逐张调:
- 跳过条件:code 为空,或 condition 含"未执行/数据缺失/池状态异常/核对交易台"(修数据类);
- verdict 返回 None → verdict_dead=True 停止后续(该批余卡无建议);busy → 只跳该票;
- 成功挂 `card["ai_verdict"] = {"verdict","why"}`,随日报 payload 落库;
- eod._generate_daily 传 `review_verdict=reviewer.review_verdict`。
- T15B:mock verdict 函数,断言技术否决卡带 ai_verdict、修数据卡不带、None 后停止调用。

## Task 3: brief 卡片改版(T15C)

`_review_card(..., ai_verdict=None)`:
- 卡加 "ai_verdict" 字段(None 或 {verdict,why});
- 带票复核卡 actions 一律换两键:`review_remove`(移出)/`review_keep`(保留);有 verdict 时对应键标 `"primary": true` 供前端主次样式;howto 按 verdict 措辞,无 verdict 时"AI 建议未生成,按上面事实自行拍板";
- 修数据类复核卡保持现状(ack)。build_brief 主循环从日报卡取 ai_verdict 传入。
- T15C:mock 日报卡,断言 actions id/primary/howto 契约。

## Task 4: service 执行端点(T15D)

`api_brief_act` 新分支:
- `review_keep`:并入留痕组(skip/ack/dismiss 同款,user_action_add 后 ok)——前端当日隐藏卡,次日机械层仍否决则再出(持续破位持续提醒,符合现状节奏);
- `review_remove`:row 缺失给人话错误;否则 user_action_add 留痕 + `api_pool_transition(removed)`。
- T15D:临时库,remove 转池成功+留痕;keep 留痕不动池;已移出票 remove 给人话错误。

## Task 5: home.html 渲染(T15E)

- cardHtml:卡有 ai_verdict → aibox(徽章 建议移出/建议保留 + why;样式照原型);无 verdict 但 actions 含 review_* → 降级灰字"AI 建议未生成…";
- 按钮:primary 主样式(keep=蓝/remove=红),回调走现有 /api/brief/act fetch 模式,成功隐藏卡+toast;
- T15E:源码断言(ai_verdict/review_remove/review_keep/aibox)。

## Task 6: 三测 + E2E + 上线

- 三测全绿(388→393±);preview 起 8788 mock 快照/日报渲染卡验证按钮通路;审查 workflow;kickstart + curl 验收;交付总结+CLAUDE.md/MEMORY.md。

## Self-Review

拍板三点分别落在 Task 3/4(按钮)、Task 1(二选一)、Task 2(覆盖范围);成本护栏对齐 interpret_event;修数据类走原路;类型 ai_verdict={verdict,why} 贯穿 reviewer→daily→brief→home→测试。
