# P1 策略插件层 · 实现计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 把 eod.py 里散落的 `if mode == "short"` 硬分叉收敛为策略注册表（strategies/），注册表只接管"逐只 scan"与"持仓出场"两处分叉，其余 mode 判断改读策略能力声明——**行为零变化，golden master 逐键 diff 为空是唯一合格证**。附带一项有意的行为改进：持仓出场按持仓行的 strategy 字段路由引擎（mid 仓永远走 mid 规则，不再被主模式劫持）。

**Architecture:** 新建 strategies/ 包（base 协议+registry+short/mid 两个包装插件）；判定函数（screener.analyze/check_positions、rules_mid.analyze_mid/check_exit_mid）**零改动零搬家**；eod 的 mid 专属编排辅助（_gain_120d/_rs_percentile/_gate_ok/_yj_latest/_check_positions_mid）搬进 strategies/mid.py 并在 eod 留兼容别名。设计依据：docs/设计-策略中心-20260711.md §4（终审收窄口径）。

**Tech Stack:** 纯 Python 重构，无新依赖，无 UI（按流程免原型）。

## Global Constraints（每任务隐含遵守）

- **无 git 仓库**："提交"=三测全绿+勾复选框。三测命令：`cd ~/云慧养/zmu_gitee/screener && .venv/bin/python test_rules.py && .venv/bin/python test_rules_mid.py && .venv/bin/python test_web.py`。基线 **8/20/428**（test_web.py 共 5338 行，末节 ㉑ P0 结束于 5334 行，新用例插在 5334 之后、5336 `print()` 之前）。
- **红线**：screener.py / rules_mid.py 判定语义与签名零改动；settings 键名零改动（P1 完全不碰 params.py 的键——`MODE`/`get_mode`/`set_mode`/`MID_KEY_MAP`/`apply()` 原样）；生产改 .py 后必须 `launchctl kickstart -k gui/$(id -u)/com.rui.stock-screener.web` 并 curl 验收（集中在最后一个任务）。
- **五条防雷铁律**（设计 §4.3，来源为对抗审查实锤）：①settings 键名一个不动；②持仓出场按持仓行 strategy 字段选引擎与取数窗（strategy 为空/unknown → 跟随当前主策略，保住 W59 老用例语义）；③ctx/参数禁用裸 `gate_ok` 名（mid 插件内部参数名用 `fundamental_gate_ok`，映射回 analyze_mid 的原参数）；④取数一律晚绑定（函数体内 `data.get_hist_closed(...)` 现取，禁止把函数对象存进注册表——test_web 的名字级 monkeypatch 靠这个活着）；⑤MODE 键退役为只读兼容（params.get_mode 仍是模式来源，run_eod_scan 内**必须保留 `params.get_mode()` 调用**——T8A2 在 3772 行 mock 的是这个函数）。
- **两个源码级断言红线**（test_web.py:2063 T5R1c 与 :4449 T11C6 用 `inspect.getsource(eod.run_eod_scan)` 断言）：`_orphan_position_alerts` 与 `_apply_gate_hysteresis` 两个调用**必须留在 run_eod_scan 函数体内**，不许挪进插件。
- **payload 契约冻结**（侦察 B 全清单）：键名/键集一字不变——mid groups 固定 7 键（capture/entry_A/entry_B/tracking/vetoed + 空垫 focus/breakout，index.html:314 直接 `G.focus.length` 零容忍）；`mode` 取值域永远 short/mid（eod.py:478 与 migrate_strategy.py 拿它当判据）；pos_alerts 行 mid 七扩展字段（exits/week_ma30/week_ma20/stop_price/entry_price/held_peak/eff_stop——daily.py:371 以 `"exits" in pa` 判引擎跑没跑）；每个 group 行必须有 `gain_pct`（eod.py:996 排序即契约）；`config` 精确 11 键、mid 才有 `config_mid`。
- **P0 足迹禁区**（eod.py 内，重构时逐字保留）：`_update_circuit_breaker`（324-373）、熔断挂点 try/except（944-949）、payload `circuit_breaker` 键（972）、`_sync_pool` 候选带 industry（526-530）、`bootstrap_candidates` 带 industry（687-689）。
- **测试新节**：编号 ㉒，节首**必须自建干净临时库**（㉑ 节 DB_PATH 未还原且 settings 残留 ACCOUNT_SIZE=1000000、data._INDUSTRY_CACHE 有 fail_until 残留——需要 get_industry 的用例先重置缓存）。用例名 TP1 前缀。

---

### Task 1: golden master 基线工具 + 重构前基线采集

**Files:**
- Create: `golden_master.py`（项目根，入库长期复用）
- Create: `reports/golden_p1_short_before.json`、`reports/golden_p1_mid_before.json`（运行产物）

**Interfaces:**
- Produces: `golden_master.py` CLI：`.venv/bin/python golden_master.py --out reports/golden_p1_short_before.json --mode short`（mid 同理）。固定 fixture 跑一场 run_eod_scan 后 dump 归一化 payload。
- 归一化规则：从快照剔除 `elapsed_sec`；全 payload 按 key 排序 json.dumps(sort_keys=True, ensure_ascii=False, indent=1)。

- [ ] **Step 1: 写工具**。fixture 逐字照抄 test_web 现成 mock 手法（short 照 test_web.py:114-123 五件套 + FAKE_HIST 结构；mid 照 test_web.py:880-908 的十一件套，含 `data.get_industry = lambda code: None`、`eod._macos_notify` 屏蔽、`data.get_yjyg/get_yjkb/get_trade_dates_gate/get_announcements/get_stock_news` 全 mock）。骨架：

```python
#!/usr/bin/env python3
"""P1 golden master:固定fixture跑一场run_eod_scan,dump归一化payload。
重构前后各跑一次,diff为空=行为零变化的合格证(三测mock掉大半真实路径,不算数)。
用法: .venv/bin/python golden_master.py --mode short --out reports/golden_p1_short_before.json
注意:全程临时库+全量mock,零网络零生产库;fixture与test_web的W系列同源,别改数值。"""
import argparse
import json
import os
import tempfile

os.environ["SCREENER_LOG_DIR"] = tempfile.mkdtemp(prefix="gm-logs-")

import pandas as pd

import storage
storage.DB_PATH = os.path.join(tempfile.mkdtemp(), "gm.db")
storage.init_db()

import data
import params
import eod
from screener import CONFIG
# ……(此处按 test_web.py:84-123 与 880-911 逐字搬 fixture 与 mock,含 FAKE_HIST/MID_HIST 数据)

def main():
    ap = argparse.ArgumentParser()
    ap.add_argument("--mode", choices=["short", "mid"], required=True)
    ap.add_argument("--out", required=True)
    a = ap.parse_args()
    _install_mocks(a.mode)          # 上述 mock 的安装函数
    params.set_mode(a.mode)
    eod.run_eod_scan()
    snap = storage.load_latest()
    snap.pop("elapsed_sec", None)
    snap.pop("created_at", None)    # storage 注入的时间戳,非行为
    with open(a.out, "w", encoding="utf-8") as f:
        json.dump(snap, f, ensure_ascii=False, sort_keys=True, indent=1)
    print(f"[golden] mode={a.mode} → {a.out}")

if __name__ == "__main__":
    main()
```

- [ ] **Step 2: 采集两份基线**（此刻代码=重构前）：

Run: `.venv/bin/python golden_master.py --mode short --out reports/golden_p1_short_before.json && .venv/bin/python golden_master.py --mode mid --out reports/golden_p1_mid_before.json`
Expected: 两个 json 落盘；mid 份里 `"mode": "mid"`、groups 7 键、`config_mid` 存在。

- [ ] **Step 3: 三测确认工具未破坏任何东西**（8/20/428，工具是独立脚本不该动生产文件）

---

### Task 2: strategies/ 包（协议+registry+两个插件+eod 兼容别名）

**Files:**
- Create: `strategies/__init__.py`、`strategies/base.py`、`strategies/short.py`、`strategies/mid.py`
- Modify: `eod.py`（只加 import 与兼容别名，本任务**不动 run_eod_scan**）
- Test: `test_web.py` ㉒ 节 TP1A

**Interfaces（Produces，Task 3 依赖）:**

```python
# strategies/base.py
class Strategy:
    """策略插件协议(P1):判定函数零搬动,插件只是编排壳。
    子类必须全量声明元数据——编排层按声明驱动,不再 if mode 硬分叉。"""
    id: str                    # "short" | "mid"(=快照 mode 键取值域,不许扩)
    name: str
    horizon: str               # "short" | "mid" | "long"
    supports_pool: bool        # 池同步/自动研究/日报是否启用(eod mid-only 下游)
    phase_label: str           # 逐只取数阶段的进度条文案(原 837 行两个文案)
    group_states: tuple        # groups 真实状态键序
    compat_empty_keys: tuple   # 兼容空垫键(mid=("focus","breakout"), short=())
    key_states: tuple          # focus_klines 注入的重点状态
    watch_order: dict          # _build_watch 的状态排序表

    def fetch_hist(self, code):            # 取数分派(晚绑定:函数体内现调 data.*)
        raise NotImplementedError
    def scan_pass1(self, code, df, acc):   # 逐只循环内:short 直接判定,mid 攒涨幅
        raise NotImplementedError
    def scan_pass2(self, hist_cache, acc, names):  # 循环后:mid 离线跑规则,short 无操作
        return []
    def check_exit(self, pos_rows, hist_cache, fetch):  # 本策略持仓的出场检查
        raise NotImplementedError
    def extra_payload(self):               # mid → {"config_mid": dict(CONFIG_MID)}
        return {}

def get(strategy_id):      # registry:id → 单例插件;未知 id 抛 ValueError
def get_active():          # params.get_mode() → get(...)(注意:调用方 run_eod_scan
                           # 必须自己调 params.get_mode() 再传入,保 T8A2 的函数级 mock——
                           # 所以真正给 eod 用的是 get(mode),get_active 仅便利入口)

def route_id(row, active_id):
    """持仓行→策略id(P1 铁律②的唯一判据实现,eod 与测试共用一处逻辑):
    mid/short 照写;空/unknown/其他值跟随当前主策略(csv导入仓与老数据的归宿)。"""
    sid = (row.get("strategy") or "").strip()
    return sid if sid in ("short", "mid") else active_id
```

- `strategies/short.py`：`fetch_hist`=函数体内 `return data.get_hist_closed(code)`；`scan_pass1`=`res = analyze(df)` 命中即补 code/name 返回单结果；`scan_pass2`=返回 []；`check_exit(pos_rows, hist_cache, fetch)`=逐字搬 eod.py:924-938 的 check_positions 调用（positions 列名映射 `["代码","名称","入场价","平台价"]`、fetch 必传断言非 None、整体 try/except 降级行）——**返回行结构与今天逐字节相同**。元数据：group_states=("focus","breakout","tracking","vetoed")、compat_empty_keys=()、key_states=("focus",)、watch_order={"focus":0,"breakout":1,"tracking":2}、supports_pool=False、phase_label="逐只拉日K并跑规则"。
- `strategies/mid.py`：**整体搬入** eod.py 的 `_gain_120d`(211-217)/`_rs_percentile`(220-227)/`_gate_ok`(230-242，**搬入时函数名改 `_fundamental_gate_ok`，铁律③**)/`_yj_latest`(245-257)/`_check_positions_mid`(260-321，含 eff_stop 复算与三种降级行，逐字搬)。`fetch_hist`=`return data.get_hist2(code, days=520)`；`scan_pass1`=攒 `acc["gains"][code]=_gain_120d(df)`（None 不入）；`scan_pass2`=逐字搬 eod.py:886-899 第二遍循环体（`analyze_mid(df, rs_pct=..., gate_ok=_fundamental_gate_ok(...))`——**传给引擎的关键字参数名仍是 gate_ok（引擎签名红线），改名的只是插件内部函数**）；`check_exit`=调搬入的 `_check_positions_mid`。元数据：group_states=("capture","entry_A","entry_B","tracking","vetoed")、compat_empty_keys=("focus","breakout")、key_states=("capture","entry_A","entry_B")、watch_order={"capture":0,"entry_A":1,"entry_B":2,"tracking":3}、supports_pool=True、phase_label="逐只拉520日长窗日K(中线)"、extra_payload=`{"config_mid": dict(CONFIG_MID)}`。
- `eod.py` 兼容别名（import strategies 后模块级）：

```python
# P1 兼容别名:mid 编排辅助已搬 strategies/mid.py,旧名保留给测试与外部引用
from strategies import mid as _strategy_mid
_gain_120d = _strategy_mid._gain_120d
_rs_percentile = _strategy_mid._rs_percentile
_gate_ok = _strategy_mid._fundamental_gate_ok
_yj_latest = _strategy_mid._yj_latest
_check_positions_mid = _strategy_mid._check_positions_mid
```

（实现前先 `grep -n "_gate_ok\|_check_positions_mid\|_rs_percentile\|_gain_120d\|_yj_latest" test_web.py` 确认测试引用面；别名保证任何 `eod._xxx` 引用不碎。）

- [ ] **Step 1: 写失败测试 TP1A**（㉒ 节，节首自建库）：

```python
# ══════════ ㉒ P1 策略插件层(计划-P1-20260712) ══════════
storage.DB_PATH = os.path.join(tempfile.mkdtemp(), "test_p1.db")
storage.init_db()
import strategies
_s_short, _s_mid = strategies.get("short"), strategies.get("mid")
check("TP1A1 注册表按id取插件且单例",
      _s_short.id == "short" and _s_mid.id == "mid"
      and strategies.get("mid") is _s_mid)
check("TP1A2 mid元数据=现有payload契约",
      _s_mid.supports_pool and _s_mid.group_states == ("capture", "entry_A", "entry_B", "tracking", "vetoed")
      and _s_mid.compat_empty_keys == ("focus", "breakout")
      and _s_mid.key_states == ("capture", "entry_A", "entry_B")
      and _s_mid.extra_payload().get("config_mid", {}).get("RS_WINDOW") == 120)
check("TP1A3 short元数据+无池能力",
      not _s_short.supports_pool and _s_short.group_states == ("focus", "breakout", "tracking", "vetoed")
      and _s_short.compat_empty_keys == () and _s_short.extra_payload() == {})
import inspect as _ins_p1
check("TP1A4 取数晚绑定(函数体现调data.*,不存函数对象)",
      "data.get_hist_closed" in _ins_p1.getsource(type(_s_short).fetch_hist)
      and "data.get_hist2" in _ins_p1.getsource(type(_s_mid).fetch_hist))
try:
    strategies.get("nope")
    _p1a5_ok = False
except ValueError:
    _p1a5_ok = True
check("TP1A5 未知策略id抛ValueError", _p1a5_ok)
check("TP1A6 route_id路由判据:mid照写/unknown跟随主策略/空值跟随主策略",
      strategies.route_id({"strategy": "mid"}, "short") == "mid"
      and strategies.route_id({"strategy": "unknown"}, "mid") == "mid"
      and strategies.route_id({}, "short") == "short")
```

- [ ] **Step 2: 跑 `grep TP1A` 确认失败**（ModuleNotFoundError: strategies）
- [ ] **Step 3: 实现四个文件 + eod 别名**（本任务 run_eod_scan 一字不动）
- [ ] **Step 4: 跑 TP1A 全绿 + 三测 8/20/434**（428+6）
- [ ] **Step 5: golden master 复跑必须仍与基线一致**（别名与新包不该改行为）：
  `for m in short mid; do .venv/bin/python golden_master.py --mode $m --out /tmp/gm_$m.json && diff /tmp/gm_$m.json reports/golden_p1_${m}_before.json && echo "$m ✓"; done`

---

### Task 3: run_eod_scan 接管改造（核心任务）

**Files:**
- Modify: `eod.py` 的 run_eod_scan（分叉点 837/846-847/852-860/886-899/924-939/950-969/994-995/1002）
- Test: golden master + 三测（本任务不加新用例——合格证是 diff 为空）

**Interfaces:**
- Consumes: Task 2 的 strategies API。
- Produces: run_eod_scan 内不再有 `analyze(`/`analyze_mid(`/`check_positions(`/`_check_positions_mid(` 直调（全走插件）；`mode` 变量仍在（=strategy.id），payload/日志/下游判断用它的地方逐字保留。

- [ ] **Step 1: 改造清单**（逐点，改完自查每点）：

1. `mode = params.get_mode()`（823）之后加 `strat = strategies.get(mode)`（**保留 get_mode 调用**，铁律⑤/T8A2）。
2. 837 phase 文案 → `_set(phase=strat.phase_label, ...)`。
3. 846-847 取数 → `df = strat.fetch_hist(code)`。
4. 852-860 循环内判定 → 

```python
                    acc.setdefault("gains", {})
                    for res in strat.scan_pass1(code, df, acc):
                        res["code"], res["name"] = code, str(name)
                        results.append(res)
```

（short 的 scan_pass1 返回 [res] 或 []；mid 返回 [] 只写 acc。`gains = {}` 原变量改为 `acc = {"gains": {}}`，注释保留原文。）
5. 886-899 第二遍 → 

```python
        _set(phase="全池RS分位+业绩闸门,跑中线规则")
        names = dict(zip(uni["代码"], uni["名称"]))
        for res in strat.scan_pass2(hist_cache, acc, names):
            results.append(res)
```

（scan_pass2 内部含 mid 的 `_set` 日志与 RS 样本 log？——**不**：`_set`/logger 属编排层。搬法：pass2 只做纯计算返回结果列表，`RS样本 %d` 日志行留在 eod 侧用 `len(acc["gains"])` 打印，保持日志输出一致。short 的 pass2 返回 []，phase 行仅 mid 打印——用 `if strat.supports_pool:` 包住 `_set(phase=...)` 与 RS 日志两行，语义等价于原 `if mode != "short"`。）
6. 924-939 持仓出场 → **按行策略路由**：

```python
        pos_rows = _positions_rows()
        # P1 铁律②:出场按建仓策略路由(mid仓永远走mid规则);strategy 空/unknown
        # 跟随当前主策略——保住老数据与 W59 语义,也是 csv 导入仓(unknown)的归宿
        by_strat = {}
        for p in pos_rows:
            by_strat.setdefault(strategies.route_id(p, mode), []).append(p)
        pos_alerts = []
        for sid, rows in by_strat.items():
            pos_alerts.extend(strategies.get(sid).check_exit(rows, hist_cache,
                                                             fetch=fetch_and_cache))
        pos_alerts.extend(_orphan_position_alerts(pos_rows))
```

前置：`_positions_rows()`（161-180）的行 dict 补 `"strategy": r.get("strategy")`（pool 行已有列；positions fallback 行同名列 P0 也加了）。**注意 fetch_and_cache 闭包定义在 901-910，改造后必须先于出场段定义——核对顺序，若原文已在前则不动。**
7. 950-969 groups/key_states → 数据驱动（输出必须逐字节等价）：

```python
        groups = {s: [x for x in results if x["state"] == s] for s in strat.group_states}
        for k in strat.compat_empty_keys:
            # 旧前端(任务4界面焕新前)直接读短线键,给空列表防页面脚本崩;
            # 中线名单不双写进旧键——数据只有一份口径
            groups[k] = []
        key_states = strat.key_states
```

8. 994-995 config_mid → `payload.update(strat.extra_payload())`。
9. 1002 mid-only 下游 → `if strat.supports_pool:`（内部 _sync_pool/锚/事件/自动研究/日报五段逐字不动）。
10. `_build_watch(..., mode=mode)` 调用**不动**（内部 order 表继续按 mode 分派——它与 strat.watch_order 数值一致，P1 不强行归一，避免无谓 diff 面；watch_order 元数据留给 P2 消费）。
11. `_prev_snapshot_states`（478）、payload `"mode": mode`（971）、日志（1042）全部不动。

- [ ] **Step 2: 三测全绿 8/20/434**
- [ ] **Step 3: golden master 终验（本任务唯一合格证）**：

Run: `for m in short mid; do .venv/bin/python golden_master.py --mode $m --out /tmp/gm_${m}_after.json && diff /tmp/gm_${m}_after.json reports/golden_p1_${m}_before.json && echo "$m 零差异 ✓"; done`
Expected: 两个 diff 输出为空。**任何一个键有差异=本任务不合格，逐键排查，禁止改基线迁就实现。**

- [ ] **Step 4: 源码级断言自查**：`.venv/bin/python -c "import inspect, eod; s = inspect.getsource(eod.run_eod_scan); print('_orphan_position_alerts' in s, '_apply_gate_hysteresis' in s, 'params.get_mode' in s)"` → `True True True`

---

### Task 4: 镜像测试与行为改进钉死

**Files:**
- Test: `test_web.py` ㉒ 节追加 TP1B/TP1C

**Interfaces:**
- Consumes: Task 3 的按行路由。

- [ ] **Step 1: 写用例**（先写——若 Task 3 实现正确应直接绿；红了说明路由有 bug，修 eod 不修测试）：

```python
# TP1B 出场按建仓策略路由:mid 持仓在两种主策略下输出逐字节一致(设计§4.3铁律②镜像测试)
storage.DB_PATH = os.path.join(tempfile.mkdtemp(), "test_p1_route.db")
storage.init_db()
_orig_p1b = (data.get_hist2, params.get_mode)
data.get_hist2 = lambda code, days=520: MID_HIST["600011"]   # 复用⑥节 mid 夹具K线
_pid_p1 = storage.pool_upsert_candidate("600011", "路由票", 10.0, "2026-07-10")
storage.pool_transition(_pid_p1, "research")
storage.pool_transition(_pid_p1, "watchlist")
storage.pool_transition(_pid_p1, "holding", entry_price=10.0, position_amt=10000,
                        stop_price=9.5, entry_date="2026-07-10")
# 注意 entry_date 必传:缺了会走 _check_positions_mid 的"脏买入日"降级行(无 exits 键),TP1B3 必挂
_rows_p1 = eod._positions_rows()
check("TP1B1 持仓行带strategy字段", _rows_p1 and _rows_p1[0].get("strategy") == "mid")
import strategies as _strats_p1
_out_as_mid = _strats_p1.get("mid").check_exit(_rows_p1, {}, fetch=None)
# 模拟"主策略=short 的日子":路由函数应仍把 mid 行判给 mid(与 eod 同一实现)
check("TP1B2 mid行在short主策略下仍归mid桶",
      _strats_p1.route_id(_rows_p1[0], "short") == "mid")
check("TP1B3 mid出场行带七扩展字段(engine_ran契约)",
      _out_as_mid and "exits" in _out_as_mid[0] and "eff_stop" in _out_as_mid[0]
      and "week_ma30" in _out_as_mid[0])
data.get_hist2, params.get_mode = _orig_p1b

# TP1C strategy=unknown 跟随主策略(csv导入仓语义)
_row_u = {"code": "600012", "name": "unknown票", "strategy": "unknown",
          "platform": 10.0, "buy_price": 10.0, "stop_price": 9.5, "buy_date": "2026-07-10"}
check("TP1C1 unknown行跟随当前主策略", _strats_p1.route_id(_row_u, "mid") == "mid")
```

- [ ] **Step 2: 三测 8/20/438**（Task 2 后 434 + 本任务 4 条；TP1A 共 6 条：428+6=434）+ golden master 复验仍零差异

---

### Task 5: 生产上线收尾

- [ ] **Step 1: 三测终验 8/20/438 + golden master 零差异终验**
- [ ] **Step 2: 重启生产** `launchctl kickstart -k gui/$(id -u)/com.rui.stock-screener.web`；curl `/api/brief` 存活 + `/api/dashboard` 正常
- [ ] **Step 3: 首晚快照对比**（下一个交易日 16:30 后）：当日快照与前一交易日快照的 `config`/`config_mid`/`rule_version`/groups 键集逐键相等（值随行情变，键与参数必须同）；`sqlite3 screener.db` 抽查 payload `mode=="mid"`
- [ ] **Step 4: CLAUDE.md 进度补记**：`**P1 策略插件层已交付(2026-07-1x)**:strategies/包(base协议+registry+short/mid插件),eod五处mode硬分叉收敛(scan两遍+出场按持仓行strategy路由:mid仓永远走mid规则,unknown跟随主策略),判定函数零搬动,mid编排辅助迁strategies/mid.py+eod兼容别名;golden master(golden_master.py+reports/golden_p1_*_before.json)重构前后逐键零差异;三测8/20/438。`

---

## 覆盖对照（设计§4 → 任务）

| 设计条目 | 任务 |
|---|---|
| §4.1 收窄口径(只接管两分叉,下游读能力声明) | Task 3 |
| §4.2 Strategy 协议+registry | Task 2 |
| §4.3 铁律①settings键名 | 全程(P1 不碰 params 键) |
| §4.3 铁律②出场按建仓策略+镜像测试 | Task 3 改造点6 + Task 4 |
| §4.3 铁律③禁裸 gate_ok | Task 2(mid 插件内 _fundamental_gate_ok) |
| §4.3 铁律④晚绑定 | Task 2 + TP1A4 |
| §4.3 铁律⑤MODE 只读兼容 | Task 3 改造点1 |
| §4.4 快照契约兼容(空键垫/取值域) | Task 3 改造点7/11 + golden master |
| §4.5 golden master 验收 | Task 1 + Task 3 Step 3 + Task 5 Step 3 |
| §4.6 backtest.py 补费用 | **已在 P0 前置完成？——否，属 P1 收尾账**:见下遗留 |

**遗留说明**：设计 §4.6"backtest.py 补双边费用+重跑短线回测"未纳入本计划——量化审查的原始约束是"**P3 之前**完成"（矩阵消费前口径统一即可），且重跑全量回测是长时网络作业与 eod 重构无耦合。列为 P2 计划的先导任务，届时与全市场日线库回填同批跑。
