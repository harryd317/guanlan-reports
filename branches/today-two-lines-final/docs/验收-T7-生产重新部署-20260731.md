# T7 生产重新部署验收报告

- 日期：2026-07-31
- 用户授权：等效环境复核通过，允许合并 main 并重新部署
- T7 分支：`f5ba70533ff444e44a3c4288d9acdd7157ed7cfe`
- 部署前 main：`782e8e942390c7a1eb60dcd1c1d47a1e98761667`
- merge 提交：`230daa6cf78837d14a961d3c4fb4d426b2cd7f2b`
- 生产 PID：`3802` → `35752`
- 回滚状态：未触发

## 结论

T7 已合并 main，并使用生产原有 LaunchAgent、工作目录和 `restart.sh` 重新部署。生产健康检查返回 `ok=true`、`match=true`、规则版本 `10`。

生产五页在桌面 `1440×900` 与手机 `390×844` 共十个视口全部通过：页面为新界面，关键内容可见，无横向溢出，无控制台错误。工程模式显示 7 个入口。数据库和生产 plist 的部署前后字节哈希一致，因此没有触发回滚。

本报告提交后仍须完成报告镜像同步并从全新克隆复核。最终回报以远端镜像提交哈希为完成凭据。

## 部署前门禁

部署前已存在并通过生产等效验收：

- 生产 LaunchAgent 克隆副本；
- 与生产相同的启动脚本、应用入口和静态文件服务方式；
- 独立端口 `18787`；
- 三套 SQLite 一致性副本只读打开；
- 生产 plist、生产数据库和副本启停前后哈希一致；
- 五页桌面与手机十图通过；
- 镜像远端提交 `4fbc6081ce0a3e0c7e4997424d952de367c81455` 已由用户复核。

## 合并与测试

main 与 T7 合并时仅 `MEMORY.md` 发生一处重复规则引用冲突。解决方式是保留 `AGENTS.md` 为规则唯一正文，`MEMORY.md` 只保留引用。代码文件自动合并无冲突。

T7 工作树测试：

- `test_rules.py`：`8/8`
- `test_rules_mid.py`：`20/20`
- `test_web.py`：`838/838`
- `test_regime.py`：`6/6`；隔离工作树没有生产 `market.db`，历史案例按既有条件跳过
- `test_production_equivalent.py`：`20/20`

合并后的 main 测试：

- `test_rules.py`：`8/8`
- `test_rules_mid.py`：`20/20`
- `test_web.py`：`838/838`
- `test_regime.py`：`9/9`
- `test_production_equivalent.py`：`20/20`

T7 工作树首次测试被两项未纳入 Git 的运行时夹具阻断：`.venv/bin/python`、`reports/golden_p1_mid_before.json` 与 `reports/backtest_mid_trades_v5params.csv`。只为测试建立只读临时符号链接后，完整测试通过；链接已删除，工作树未改源码。

## 部署方式与健康检查

生产 plist 字节未改，工作目录继续是：

```text
~/云慧养/zmu_gitee/screener
```

部署命令：

```text
./restart.sh
```

脚本通过 `launchctl kickstart -k gui/<uid>/com.rui.stock-screener.web` 重启，并用 `/api/health` 校验启动代码与磁盘代码指纹一致。重启后 8787 监听进程为 PID `35752`；`/`、`/pool`、`/trade`、`/strategy`、`/settings` 均返回 HTTP 200。

## 数据库与 plist 哈希

部署前、重启后、十图访问后三次结果一致：

| 对象 | SHA-256 |
| --- | --- |
| 生产 plist | `93e862839f8a54ee25d2aefc704742eb82ba106cd7b5239342b5245fc2a42dac` |
| `S3_research.db` | `bf7aef744b05a69a16416c32f10663865a798f77ad828f93de54e6421018fb7b` |
| `bt_cache.db` | `d8679bfe45aa350d272b24bb3120a78f4b722a6948a148973bb8899bb78ef47e` |
| `market.db` | `a504f873eb204bee722082b1d6fec92b04e950cbae16842d4c4917a820c75fc1` |
| `screener.db` | `8c52821caefacc0134e25f36ee7071d6421ae33f48bff5fab45c3212e10fe839` |
| `market.db-wal` | `e3b0c44298fc1c149afbf4c8996fb92427ae41e4649b934ca495991b7852b855` |
| `market.db-shm` | `fd4c9fda9cd3f9ae7c962b0ddf37232294d55580e1aa165aa06129b8549389eb` |
| `screener.db-wal` | `e3b0c44298fc1c149afbf4c8996fb92427ae41e4649b934ca495991b7852b855` |
| `screener.db-shm` | `fd4c9fda9cd3f9ae7c962b0ddf37232294d55580e1aa165aa06129b8549389eb` |

机器清单：

```text
reports/t7-production-redeploy-20260731/before-hashes.json
reports/t7-production-redeploy-20260731/after-hashes.json
```

## 五页双视口验收

截图目录：

```text
docs/screenshots/t7-production-redeploy/20260731/
```

| 页面 | 桌面 | 手机 | DOM 横向溢出 | 控制台错误 | 目视结果 |
| --- | --- | --- | ---: | ---: | --- |
| 今天 | 1440×900 | 390×844 | 0 | 0 | 新界面正常，无空白 |
| 股票池 | 1440×900 | 390×844 | 0 | 0 | 新界面正常 |
| 我的 | 1440×900 | 390×844 | 0 | 0 | 新界面正常，无挤压 |
| 工程模式 | 1440×900 | 390×844 | 0 | 0 | 新界面正常，7 个入口可见 |
| 设置 | 1440×900 | 390×844 | 0 | 0 | 新界面正常，无挤压 |

十张图片均为真实 JPEG 字节，桌面图像像素精确为 `1440×900`，手机精确为 `390×844`。截图调用使用普通 `tab.screenshot()`，未传 `fullPage`。逐图 SHA-256、DOM 矩形和控制台日志位于：

```text
reports/t7-production-redeploy-20260731/visual-evidence.json
```

## Server酱

部署后发送测试消息：

```text
[观澜] T7生产重新部署验收通过
```

返回 `ok=true`、`channel=serverchan`，错误为空。SendKey 未进入对话、报告或日志。

## 回滚判定

预设回滚目标为部署前 main `782e8e942390c7a1eb60dcd1c1d47a1e98761667`。触发条件包括：重启失败、健康指纹不一致、任一页面异常、手机挤压、横向溢出、控制台错误、数据库或 plist 哈希变化。

上述条件均未出现，因此未执行回滚。
