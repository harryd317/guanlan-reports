# 任务17 · 全站重设计一期(首页驾驶舱 + 统一导航)实施计划

> **执行方式:** 子代理 TDD 逐任务执行(superpowers:subagent-driven-development),步骤用 `- [ ]` 勾选跟踪。
> **原型(已拍板,视觉唯一依据):** `docs/原型-全站重设计-方案AB-今日驾驶舱-20260714.html`(第二稿,含屏1b盘中形态)

**目标:** 首页按方案AB「今日驾驶舱」重构(hero红绿灯卡+今日结论句+左待办右速览双栏+移动端底部Tab),全站九页统一顶栏导航;判定引擎零改动。

**架构:** 纯呈现层改造。新增共享导航组件 `static/nav.js`(service.py 加一条只读路由),各页删旧导航换占位;`brief.py` 的 `waiting` 填充做最小只读扩展(等买点全量),首页右栏复用既有 `/api/intraday` 客户端 join。

**技术栈:** 原生 HTML/CSS/JS 单文件、FastAPI FileResponse、无构建链。

## 全局约束(每个任务默认适用)

- `screener.py` 的 `analyze()`/`check_positions()` 一行不改;`eod.py`/`rules_mid.py`/`regime.py`/`strategies/*` 判定链四文件零改动(指纹清单 `fingerprint.py:14`)
- `test_rules.py` 只加不改;test_web 既有用例只加不改(逐条回归锁不许动)
- 铁律6:盘中措辞只许「临近/等收盘确认」,禁「已触发/已确认」
- 时区一律 `ZoneInfo("Asia/Shanghai")`;服务只绑 127.0.0.1:8787;页面单文件无构建链
- 改任何 `.py` 后必须 `./restart.sh`(kickstart+指纹断言);只改 static/*.html 免重启
- 每任务收尾必跑三测:`.venv/bin/python test_rules.py && .venv/bin/python test_rules_mid.py && .venv/bin/python test_web.py && .venv/bin/python test_regime.py`
- 每任务一个 git 提交;全部完成后推送 github-personal
- **home.html 十五条"必须原样保留"清单(重构红线,见附录A)**

---

### 任务1: 统一导航组件 nav.js + 全站九页接入

**Files:**
- Create: `static/nav.js`
- Modify: `service.py`(约 208 行附近加一条路由)
- Modify: `static/index.html:185-193`、`static/pool.html:160-167`、`static/daily.html:157-164`、`static/trade.html:153-159`、`static/settings.html:153-158`、`static/history.html:170-175`、`static/strategy.html:39-43`、`static/stock_detail.html:180-186`、`static/ask.html:152-156,208-218`
- Test: `test_web.py`(新增 ㉖ 节 `T17A×6`)

**Interfaces:**
- Produces: `GET /nav.js` 返回 JS;页面占位约定 `<header id="site-nav" data-here="today|pool|ask|trade|strategy|settings|"></header>`;nav.js 暴露右侧插槽 `#nav-slot`(任务3 首页时钟用)

**导航项定死(与原型一致,六项):** 今日`/` · 股票池`/pool` · 问AI`/ask` · 交易台`/trade` · 策略`/strategy` · 设置`/settings`;移动端(≤680px)顶栏隐藏项目,显示底部 Tab 四项:今日`/`、池子`/pool`、问AI`/ask`、我的`/trade`。日报不进顶栏(首页「完整日报 →」与 daily 页可达);`/pro`、`/history` 入口一期挪到 settings 页「维修间」小节。

- [ ] **Step 1: 写失败测试(test_web ㉖ 节头 + T17A)**

```python
# ㉖ 任务17 全站重设计一期 —— 统一导航
_src_nav = open(os.path.join(BASE, "static", "nav.js"), encoding="utf-8").read() if os.path.exists(os.path.join(BASE, "static", "nav.js")) else ""
check("T17A1 nav.js 存在且含六项导航定义",
      all(k in _src_nav for k in ['"今日"', '"股票池"', '"问AI"', '"交易台"', '"策略"', '"设置"'])
      and all(p in _src_nav for p in ['"/pool"', '"/ask"', '"/trade"', '"/strategy"', '"/settings"']))
check("T17A2 nav.js 含移动端底部Tab四项与断点",
      '"池子"' in _src_nav and '"我的"' in _src_nav and "680px" in _src_nav)
check("T17A3 service 挂 /nav.js 只读路由",
      '"/nav.js"' in inspect.getsource(service) and "nav.js" in inspect.getsource(service.nav_js))
_nav_pages = {"index.html":"", "pool.html":"pool", "daily.html":"", "trade.html":"trade",
              "settings.html":"settings", "history.html":"", "strategy.html":"strategy",
              "stock_detail.html":"", "ask.html":"ask"}
_ok_holes, _bad = True, []
for _f, _here in _nav_pages.items():
    _s = open(os.path.join(BASE, "static", _f), encoding="utf-8").read()
    if 'id="site-nav"' not in _s or '/nav.js' not in _s:
        _ok_holes = False; _bad.append(_f)
check("T17A4 九页均接入 site-nav 占位+nav.js", _ok_holes, ",".join(_bad))
_src_home17 = open(os.path.join(BASE, "static", "home.html"), encoding="utf-8").read()
check("T17A5 首页也走统一导航(data-here=today)", 'data-here="today"' in _src_home17)
check("T17A6 旧双世界导航清除:ask 页不再有齿轮弹层维修间",
      "workshop" not in open(os.path.join(BASE, "static", "ask.html"), encoding="utf-8").read())
```

- [ ] **Step 2: 跑测确认失败** — `.venv/bin/python test_web.py`,预期 T17A1~A6 FAIL(nav.js 不存在)
- [ ] **Step 3: 写 nav.js(完整实现)**

```javascript
/* nav.js — 全站统一顶栏+移动端底部Tab(任务17)。零依赖,零构建。
   用法:<header id="site-nav" data-here="pool"></header><script src="/nav.js" defer></script>
   data-here 留空 = 无高亮(详情页/历史页)。#nav-slot 是右侧插槽(首页时钟往里写)。 */
(function () {
  var ITEMS = [["今日", "/", "today"], ["股票池", "/pool", "pool"], ["问AI", "/ask", "ask"],
               ["交易台", "/trade", "trade"], ["策略", "/strategy", "strategy"], ["设置", "/settings", "settings"]];
  var TABS = [["今日", "/", "today", "◉"], ["池子", "/pool", "pool", "▤"],
              ["问AI", "/ask", "ask", "✦"], ["我的", "/trade", "trade", "▥"]];
  var css = ""
    + ".unav{background:rgba(255,255,255,.92);-webkit-backdrop-filter:blur(14px);backdrop-filter:blur(14px);"
    + "border-bottom:1px solid #E7E8EC;position:sticky;top:0;z-index:50}"
    + ".unav .in{max-width:1060px;margin:0 auto;padding:0 18px;display:flex;align-items:center;gap:18px;height:54px}"
    + ".unav .brand{font-size:16px;font-weight:800;letter-spacing:.5px;white-space:nowrap;color:#17181C;text-decoration:none}"
    + ".unav .brand i{display:inline-block;width:9px;height:9px;border-radius:50%;background:#B45309;margin-right:7px;vertical-align:1px}"
    + ".unav nav{display:flex;gap:4px;flex:1}"
    + ".unav nav a{padding:6px 13px;border-radius:20px;text-decoration:none;color:#82868F;font-size:14.5px;white-space:nowrap}"
    + ".unav nav a.on{background:#17181C;color:#fff;font-weight:600}"
    + ".unav nav a:hover:not(.on){background:#EEEFF2;color:#17181C}"
    + ".unav .slot{font-size:13px;color:#82868F;white-space:nowrap;margin-left:auto}"
    + ".utab{display:none;position:fixed;left:0;right:0;bottom:0;z-index:60;background:rgba(255,255,255,.95);"
    + "-webkit-backdrop-filter:blur(14px);backdrop-filter:blur(14px);border-top:1px solid #E7E8EC;"
    + "padding:7px 8px calc(7px + env(safe-area-inset-bottom))}"
    + ".utab .in{display:flex}"
    + ".utab a{flex:1;text-align:center;font-size:11px;color:#82868F;text-decoration:none;line-height:1.25}"
    + ".utab a em{display:block;font-style:normal;font-size:20px}"
    + ".utab a.on{color:#0A6CFF;font-weight:700}"
    + "@media (max-width:680px){.unav nav{display:none}.utab{display:block}body{padding-bottom:76px}}";
  var st = document.createElement("style"); st.textContent = css; document.head.appendChild(st);
  var host = document.getElementById("site-nav"); if (!host) return;
  var here = host.getAttribute("data-here") || "";
  function esc(s) { return String(s).replace(/[&<>"]/g, function (c) { return ({"&":"&amp;","<":"&lt;",">":"&gt;",'"':"&quot;"})[c]; }); }
  var links = ITEMS.map(function (it) {
    return '<a href="' + it[1] + '"' + (it[2] === here ? ' class="on"' : "") + ">" + esc(it[0]) + "</a>";
  }).join("");
  host.className = "unav";
  host.innerHTML = '<div class="in"><a class="brand" href="/"><i></i>纪律选股</a><nav>' + links
                 + '</nav><span class="slot" id="nav-slot"></span></div>';
  var tabs = TABS.map(function (t) {
    return '<a href="' + t[1] + '"' + (t[2] === here ? ' class="on"' : "") + "><em>" + t[3] + "</em>" + esc(t[0]) + "</a>";
  }).join("");
  var bar = document.createElement("div"); bar.className = "utab";
  bar.innerHTML = '<div class="in">' + tabs + "</div>";
  document.body.appendChild(bar);
})();
```

- [ ] **Step 4: service.py 加只读路由**(放在 `page_pro` 附近,约 216 行后)

```python
@app.get("/nav.js")
def nav_js():
    """统一导航组件(任务17)。只读静态文件,不进指纹清单。"""
    return FileResponse(os.path.join(BASE, "static", "nav.js"), media_type="application/javascript")
```

- [ ] **Step 5: 九页替换旧导航**。每页操作前先 `grep` test_web 是否钉住了将删除的子串(如 `grep -n '<h1>交易台' test_web.py`),**凡被既有用例断言的字符串必须保留**(把 `<h1>` 移到 nav 占位下方而不是删掉)。逐页把侦察确认的旧导航块整段替换为:

```html
<header id="site-nav" data-here="pool"></header>
<script src="/nav.js" defer></script>
```

(data-here 按页填:index→""、pool→"pool"、daily→""、trade→"trade"、settings→"settings"、history→""、strategy→"strategy"、stock_detail→""、ask→"ask";stock_detail 保留正文内「← 返回严选池」链接但移出导航条;ask 删除齿轮弹层 208-218 行的维修间链接组,其 gear 账户编辑功能 ask 页本来没有,直接删弹层;strategy 页顺带核对其内联 token 不再被导航依赖)

- [ ] **Step 6: settings 页加「维修间」小节**(旧版入口不丢,功能覆盖清单第10项):

```html
<div class="card"><h2>维修间</h2>
  <p class="mu">给维护者/怀旧用的旧入口,日常用不到。</p>
  <a href="/pro">旧版专业看板 →</a> · <a href="/history">历史快照 →</a> · <a href="/daily">完整日报 →</a>
</div>
```

- [ ] **Step 7: 跑三测确认 T17A 全绿且既有用例零回归**
- [ ] **Step 8: `./restart.sh`(改了 service.py)+ 浏览器抽查三页导航渲染与高亮**
- [ ] **Step 9: Commit** — `git add -A && git commit -m "任务17-1: 统一导航 nav.js 全站接入"`

---

### 任务2: brief.waiting 扩展 —— 等买点全量进首页速览

**Files:**
- Modify: `brief.py:661-671`(pool_rows 第二循环)
- Test: `test_web.py`(`T17B×4`)

**Interfaces:**
- Produces: `/api/brief` 的 `waiting[]` 语义扩为「日报 HOLD/WATCH 卡票 ∪ 池内全部 state=watchlist 票(去重)」,行结构不变 `{code,name,note}`;新增票 note 固定为 `"等买点"`。前端(任务3)靠既有 `INTRA_MAP` join 实时五项,join 不到时只显示名字+note。

**背景(侦察结论):** 现 waiting 只在日报出 HOLD/WATCH 卡时填(`brief.py:650-655`),今天 10 只等买点票 waiting 为空,首页整段不渲染——这是"空态死气"的根源之一。扩展点选 pool_rows 循环(方案A):纯读、不碰 `eod._build_watch`(指纹文件)、golden master 无感。

- [ ] **Step 1: 写失败测试**

```python
# T17B waiting 扩展:池内 watchlist 全量可见
_bb_src = inspect.getsource(brief.build_brief)
check("T17B1 build_brief 在 pool_rows 循环补 watchlist 进 waiting",
      "watchlist" in _bb_src and _bb_src.count("waiting_rows.append") >= 2)
_rep0 = {"date": "2026-07-14", "cards": [], "positions": []}
_pool0 = [{"code": "688072", "name": "拓荆科技", "state": "watchlist"},
          {"code": "600584", "name": "长电科技", "state": "research"}]
_out0 = brief.build_brief(_rep0, pool_rows=_pool0, reviews=[], snap={"index": {"ok": True}})
_wcodes = [w["code"] for w in _out0["waiting"]]
check("T17B2 watchlist 票无卡也进 waiting 且带缺省 note",
      "688072" in _wcodes and any(w["code"] == "688072" and w.get("note") == "等买点" for w in _out0["waiting"]))
check("T17B3 非 watchlist 状态不进 waiting", "600584" not in _wcodes)
_rep1 = {"date": "2026-07-14", "positions": [],
         "cards": [{"code": "688072", "name": "拓荆科技", "next_action": {"type": "WATCH", "note": "回踩临近"}}]}
_out1 = brief.build_brief(_rep1, pool_rows=_pool0, reviews=[], snap={"index": {"ok": True}})
check("T17B4 已有 HOLD/WATCH 卡的票不重复进 waiting",
      [w["code"] for w in _out1["waiting"]].count("688072") == 1)
```

(注:`build_brief` 实参名以 `brief.py:525` 真实签名为准,执行者先 `grep -n "def build_brief" brief.py` 核对后再落测试,**测试里的调用方式必须能真跑**,不许 mock 掉被测逻辑)

- [ ] **Step 2: 跑测确认 T17B1/B2/B4 FAIL**
- [ ] **Step 3: 最小实现** —— 在 `brief.py` pool_rows 第二循环内(new/research 补卡逻辑旁)追加:

```python
        # 任务17:等买点票即使今天没出卡,也进 waiting(首页速览常驻,空态不空屏)
        if str(row.get("state") or "") == "watchlist" and code not in waiting_seen:
            waiting_rows.append({"code": code, "name": name, "note": "等买点"})
            waiting_seen.add(code)
```

并在两处 `waiting_rows.append` 的第一处同步维护 `waiting_seen`(循环前初始化 `waiting_seen = set()`,首循环 append 后 `waiting_seen.add(code)`)。变量名如与现场冲突,以现场为准微调,语义不变。

- [ ] **Step 4: 跑三测全绿**(重点看 T7 系列 waiting 相关旧用例零回归;若有旧用例因 waiting 变多而挂,**停下来评估语义而不是改旧用例**——按全局约束旧用例不许改,需要时给新增行为加开关参数再议)
- [ ] **Step 5: `./restart.sh` + `curl -s http://127.0.0.1:8787/api/brief | python3 -c "import json,sys; d=json.load(sys.stdin); print(len(d['waiting']), d['waiting'][:2])"` 预期 waiting≈10**
- [ ] **Step 6: Commit** — `git commit -am "任务17-2: brief.waiting 扩展等买点全量"`

---

### 任务3: home.html 重构 —— 今日驾驶舱

**Files:**
- Modify: `static/home.html`(布局/样式重写,逻辑函数保留)
- Test: `test_web.py`(`T17C×8` + `T17D×6`)

**Interfaces:**
- Consumes: 任务1 的 `#site-nav`/`#nav-slot`;任务2 的 `waiting` 全量;既有 `/api/brief`、`/api/intraday`(`INTRA_MAP`)、`/api/regime/current`(`REGIME.regime_cn/gate_line`)、`/api/trade/overview`(账户总额)
- Produces: 新首页 DOM 骨架(hero/#cols 双栏/pool 速览),十五条保留逻辑原样可用

**布局映射(原型 → 实现),自上而下:**
1. `#site-nav`(nav.js)+ 时钟走 `#nav-slot`:`tickClock()` 改为优先写 `document.getElementById("nav-slot")`,不存在再回落 `#clockbar`(保留旧元素防 nav.js 加载失败白屏)
2. **hero 红绿灯卡**(新增渲染函数 `heroHtml(d)`,在 `render()` 顶部调用):
   - 左主区:`今日市况·X月X日收盘判定` 小标 + `REGIME.regime_cn` 大字(琥珀)+ 门 pill(开=绿「开仓门:开」/关=红「开仓门:关」)+ **结论句** + `gate_line` 白话行(带「为什么? →」链 `/strategy`)+ 门缝进度条(`mg.close/mg.ma/mg.to_open_pct`,宽度 `Math.max(4, Math.min(100, 100-系数*to_open_pct))` 简化为 `100-to_open_pct*13` 夹在 4~100)
   - 右侧栏三行:今天要做的事(`visibleCards.length` 件)/账户(`ACCT` 来自 loadAcct,取不到显示「—」)/系统状态(`is_stale`→黄字「数据过期」+按钮沿用 rescan;`circuit_breaker`→红;否则绿「✓ 数据新鲜 · 熔断未触发」)
   - **熔断态:hero 整卡红色主题**,大字区改「回撤熔断 · 已停新仓」,原 haltbar 文案(恢复入口在设置页)并入卡内;市况小字保留
   - REGIME 未到手时 hero 降级:大字位显示门状态文案(现有 622-624 的机械句),不空白
3. **结论句查表(新增 `verdictText(d)`,机械拼接,禁 AI):**

```javascript
function verdictText(d, visibleCards) {
  if (d.circuit_breaker) return "→ 今天:已熔断,不开新仓;持仓照常按卖出线管理。";
  var gate = d.market_gate || {};
  var a = gate.open ? "名单可执行" : "不买新票";
  var h = (d.holdings || []).length;
  var b = h > 0 ? ("盯好 " + h + " 只持仓的卖出线") : "没有持仓要管";
  var c = visibleCards.length > 0 ? ("有 " + visibleCards.length + " 件事要处理 ↓") : "看看就好";
  return "→ 今天:" + a + "," + b + "," + c + "。";
}
```

4. **双栏 `#cols`**(CSS grid `1.55fr 1fr`,≤900px 单列):左=今日待办卡流(`cardHtml` 全家 + 复核折叠 + 除权灰条 + `calm` 安心条:仅当 `!gate.open && visibleCards.length===0` 或全部处理完时显示「没别的了…」/「都处理完了 ✅」,沿用既有两分支语义);右=等买点速览卡(`waitingHtml` 改造:行结构=名/收盘价+涨跌/「离买点差 X%」+量比/`near` 琥珀标「回踩临近」(收盘态)或「临近 · 等收盘确认」(盘中态),join 不到 INTRA 的票只显示名字+note)+ 我持有的卡(`holdingsHtml` 迁入,空态文案+「+ 录入已有持仓」链 `/trade`、「📷 截图批量记账」链 `/trade`)
5. **样式**:整体替换 `<style>` 为原型的设计令牌体系(原型 `:root` 块原样搬),旧类名(`.card/.pos/.row/.light/.mngbar/.pricebar/.ocrfilled` 等)**保留类名只改观感**,JS 零改名
6. **删除 gearpanel**(264-285)与 `showGear`:账户编辑入口并入 hero 右栏「账户」行(点击跳 `/trade`);`loadAcct` 保留(hero 要显示总额)。`saveAcct` 一并删除(交易台已有同功能)。**删除前 grep test_web 对 gearpanel/saveAcct 的既有断言,凡被钉住的先在新结构里等价保留再删旧的**(侦察已知 T10 系列钉过齿轮瘦身,执行者必须逐条核对)
7. **杜绝行话**:「距平台」字样在 home.html 内全部换成「离买点差」;等宽数字类 `.num` 沿用

- [ ] **Step 1: 写失败测试**

```python
# T17C 首页驾驶舱 DOM 契约
_h = open(os.path.join(BASE, "static", "home.html"), encoding="utf-8").read()
check("T17C1 hero 渲染函数存在且含市况大字与门pill", "function heroHtml" in _h and "gatepill" in _h)
check("T17C2 结论句机械查表存在且三分支齐全",
      "function verdictText" in _h and "不买新票" in _h and "名单可执行" in _h and "看看就好" in _h)
check("T17C3 熔断态整卡红+不开新仓话术", ("回撤熔断" in _h) and ("不开新仓" in _h))
check("T17C4 门缝进度条绑定 to_open_pct", "to_open_pct" in _h and "gaterow" in _h)
check("T17C5 双栏骨架与右栏速览", 'id="cols"' in _h or 'class="cols"' in _h)
check("T17C6 等买点行白话「离买点差」且无「距平台」", "离买点差" in _h and "距平台" not in _h)
check("T17C7 盘中tag守铁律6", "等收盘确认" in _h and "已触发" not in _h and "已确认" not in _h)
check("T17C8 时钟优先写 nav-slot 并保留 clockbar 回落",
      "nav-slot" in _h and "clockbar" in _h)
# T17D 保留逻辑钉死(重构防丢)
check("T17D1 防吞输入三选择器仍在", '.pricebar.open,.learn.open,.mngbar' in _h)
check("T17D2 defaultPrice 仍只认买点fact", "function defaultPrice" in _h and "买点" in _h)
check("T17D3 OCR 隐私文案与 ocrbtn 不带 cardbtn", "ocrpriv" in _h and 'class="ocrbtn"' in _h)
check("T17D4 行内管理三件套仍在", "mngSold" in _h and "mngRemove" in _h and "cooldown:false" in _h.replace(" ", ""))
check("T17D5 本地隐藏与静默留痕仍在", "LOCAL_ACTIONS" in _h and "logLocalAct" in _h)
check("T17D6 标题/数据日双源仍分离", "fmtTodayCN" in _h and "fmtDay" in _h)
```

- [ ] **Step 2: 跑测确认 T17C1~C6 FAIL(T17D 此刻应 PASS,作为重构过程的守护)**
- [ ] **Step 3: 重构实现**(一次只动一个区块,每动完跑一次 `test_web.py` 盯 T17D 不掉绿):顺序=样式令牌 → nav 接入+时钟 → heroHtml/verdictText → 双栏骨架 → waitingHtml 改造 → holdingsHtml 迁移 → gearpanel 移除
- [ ] **Step 4: 三测全绿**
- [ ] **Step 5: 浏览器实测(免重启,刷新即可):** 桌面 1280px + 手机 375px 各截图;断言渲染层(吸取渲染层盲区教训,不许只看 API):hero 大字=「冲高有险」、结论句完整、右栏≈10行、无横向滚动、复核灰条可展开、「9 条」数字正确
- [ ] **Step 6: Commit** — `git commit -am "任务17-3: 首页重构为今日驾驶舱"`

---

### 任务4: 全站 E2E 验收 + 交付

**Files:** 无新改动(只验收;发现缺陷回上面任务修)

- [ ] **Step 1: 三测最终跑**,记录用例总数(预期 8 / 20 / 565±,test_regime 全绿)
- [ ] **Step 2: `./restart.sh`**(确认指纹 match=true,即使本任务只改了静态文件也做一次终态校验)
- [ ] **Step 3: 有头浏览器 E2E(用户偏好可视化):** 依次过 `/`、`/pool`、`/ask`、`/trade`、`/strategy`、`/settings`、`/daily`、`/history`、`/pro`、`/stock/688072`:①每页统一顶栏渲染且高亮正确(index/pro 高亮错位这个存量 bug 应当消失)②首页盘中形态留到下一交易日 9:30-15:00 抽查一次(50秒刷新+「等收盘确认」tag),当晚先用收盘态验收③375px 手机宽度过 `/`、`/pool`、`/trade` 三页
- [ ] **Step 4: 截图汇报给用户**(桌面首页全屏 + 手机首页 + 任一子页导航)
- [ ] **Step 5: 更新 CLAUDE.md 任务17 一段(交付状态+测数)与交付总结 `docs/交付总结-17-全站重设计一期-20260714.md`**
- [ ] **Step 6: Commit + push** — `git add -A && git commit -m "任务17: 全站重设计一期交付" && git push`

---

## 附录A · home.html 十五条必须原样保留(重构红线)

1. 铁律6措辞:`distChip`/`liveRowHtml` 盘中只说「等收盘确认」,收盘改口「看今天的卡」
2. 50秒刷新防吞输入:`.pricebar.open,.learn.open,.mngbar` 任一开着跳过重渲
3. 标题(`fmtTodayCN` 真实今天)与数据日(`fmtDay`)双源分离
4. 两种「无操作」分支:全无卡「今天无操作」vs 处理完「都处理完了 ✅」,件数按 visibleCards
5. 本地隐藏:`todayHideKey/isHidden/markHidden` + `LOCAL_ACTIONS` 只隐藏+静默留痕
6. `defaultPrice` 只认「买点」fact 优先今收,取不到留空
7. 动作分类:`PRICE_ACTIONS/LOCAL_ACTIONS`;`bought_violate`→`bought`+`gate_violate:true`
8. 仓位硬闸:金额必填、万→元×10000、`need_force` 二次确认
9. `setCardBusy` 防连点;📷 `.ocrbtn` 不带 `cardbtn` 类、自管「识别中…」
10. OCR 隐私:内存压缩不落盘、`.ocrpriv` 文案、「核对后再确认」提示
11. 行内管理:`mngSold` 带 `cooldown:false`;`remove_wrong` 红字二次确认
12. 账户换算:万/亿→元(此逻辑随 saveAcct 删除迁往交易台,交易台已有,首页只读显示)
13. 时钟阶段:优先 `INTRA.phase`,closed 用 `INTRA.trading_day`,禁纯本机时间
14. REGIME/盘中/账户任一取不到均静默降级,不白屏
15. `esc()` 转义贯穿一切插值

## 附录B · 侦察报告结论索引(执行者需要背景时看)

- 首页结构/函数/端点清单:本计划由三份侦察报告支撑,关键行号已内嵌各任务
- waiting 语义与三端点对照:`/api/intraday` rows 是技术监控名单(84只),与池 watchlist(10只)不保证一一对应——右栏 join 不到就降级显示名字,这是**设计内行为**
- 导航现状三套范式与九页旧块行号:见任务1 Step 5
- test_web 断言方式=读文件断子串+直调 service 函数;新用例沿用 `check()` 风格,章节 ㉖
