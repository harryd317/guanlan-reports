# 任务13 · 体检回放腿明细 + 首字母搜索 实现计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** ①回放表把 T1 减半交易摊开成"主行总账+子行两笔明细"(用户查长川科技时 19.9% 对不上账的表达问题);②查任意票支持中文名/拼音首字母联想(Tushare cnspell 实测可用)。

**Architecture:** 回测引擎只加输出列(判定语义零改动)→ 重跑回放生成新 CSV(v5 参数经 params.apply() 注入,已验证 12/5/18)→ service 透传 + 新搜索端点 → 前端联想下拉 + 子行渲染。

**拍板结论(2026-07-10)**:主行总账+灰底子行;搜索规则=6位数字直查/其余候选≤8条/回车唯一候选直查;接受回放重跑更新到最新交易日(旧 CSV 备份)。原型:docs/原型-体检腿明细与搜索-20260710.html。

## Global Constraints

- `screener.py` analyze/check_positions 判定语义不动;`rules_mid.py` 不动;`test_rules*.py` 只加不改;
- backtest_mid.py 只加输出字段,`_trade_row` 的 pnl 公式零改动;
- **重跑回放前必须 params.apply() 并断言 CONFIG_MID 三键 = 12/5/18(v5 冻结参数)**;旧 v5params.csv 先备份;
- 名录拉取失败必须优雅降级(联想不可用,输代码照常);
- 完工三测:test_rules 8 / test_rules_mid 20 / test_web 379(376+3)。

---

### Task 1: backtest_mid.py legs 带日期 + trim 两列

**Files:** Modify `backtest_mid.py`(_trade_row 约 L91;模拟层三处 legs.append 约 L345/L352/L363)

- [ ] legs 元素 (weight, price) → (weight, price, date):
  - EXIT 分支:`legs.append((weight, fill["price"], str(df.iloc[fill["i"]]["date"])))`
  - TRIM 分支:`legs.append((weight / 2, fill["price"], str(df.iloc[fill["i"]]["date"])))`
  - 持有中分支:`legs.append((weight, float(df.iloc[-1]["close"]), str(df.iloc[-1]["date"])))`
- [ ] `_trade_row`:解包 `for w, p, _ in legs`;dict 增 `trim_date=legs[0][2] if len(legs)>1 else None`、`trim_price=legs[0][1] if len(legs)>1 else None`
- [ ] `grep -n "legs" backtest_mid.py` 确认无其他解包点
- [ ] 用 300604 复算脚本(本会话已验证过的)快速自测三元组路径

### Task 2: 重跑回放(v5),新 CSV 就位

- [ ] 备份:`cp reports/backtest_mid_trades_v5params.csv reports/backtest_mid_trades_v5params.csv.bak-20260710`
- [ ] 重跑脚本(后台):`params.apply()` → 断言三键 12/5/18 → `backtest_mid.run(skip_warm=False, scan=None)`
- [ ] 产出 `backtest_mid_trades_{stamp}.csv` 复制为 `backtest_mid_trades_v5params.csv`
- [ ] 验收:长川科技 300604 行 trim_date=2026-04-29、trim_price≈178.76、pnl_pct≈19.9;总笔数与 11i 报告(25笔)同数量级(窗口延伸略有出入,拍板已接受)

### Task 3: test_web 新契约(先红)

**Files:** Modify `test_web.py`(T11J3 后追加 T13A/B/C)

- [ ] T13A:monkeypatch `data.get_stock_directory` 返回 [{code:"300604",name:"长川科技",spell:"cckj"},...],断言 `service.api_stock_search(q="cckj")` 命中 300604、`q="长川"` 命中、`q="3006"` 前缀命中、`q="c"`(<2字符)返回空 hits
- [ ] T13B:临时 CSV(含 trim_date/trim_price 列 + 一行空值)→ `service._stock_replay_of("300604", path)` 断言 trim 字段透传、空串→None 兼容
- [ ] T13C:ask.html 源码断言含 `stock_search`、`check-sugg`、`trim_price`、`leglist`
- [ ] 跑 test_web:恰好 T13A/B/C 三条红(376+3,过 373+3=379 总数)

### Task 4: data.py 名录 + service.py 端点/透传(绿)

**Files:** Modify `data.py`(_stock_names 旁新增)、`service.py`(_stock_replay_of + 新端点)

- [ ] `data.get_stock_directory()`:stock_basic 拉 `ts_code,symbol,name,cnspell`,返回 [{code,name,spell(小写)}];日级内存缓存(today_cn 键),拉取失败回退旧缓存、空缓存返回 [];顺带 update _ts_code_map
- [ ] `service.api_stock_search(q)`:q 清洗小写;len<2 → 空;纯数字→code 前缀;纯 ASCII→spell 前缀;含中文→name 包含;截前 8,返回 {ok, hits:[{code,name}]}
- [ ] `_stock_replay_of` trades 增 `trim_date`(空串→None)、`trim_price`(float 或 None)
- [ ] test_web:T13A/B 转绿

### Task 5: ask.html 联想 + 子行渲染(绿)

**Files:** Modify `static/ask.html`

- [ ] HTML:searchbar 外包 `.searchwrap` + `<div class="sugg" id="check-sugg">`;CSS 从原型抄 .searchwrap/.sugg/.leglist,placeholder 改「输代码/名称/首字母,如 600519 或 cckj」(宽度实测过再定稿)
- [ ] JS 联想:input 事件 debounce 250ms → `/api/stock_search`,代次守卫(SUGG_SEQ);候选按钮 onclick=suggPick(code)→填码+checkStock;点击 searchwrap 外关闭;失败静默(联想降级,不打扰)
- [ ] checkStock 入口改:非 6 位数字时,若当前候选唯一 → 取其 code;否则 toast「从候选里点一只,或输 6 位代码」
- [ ] renderCheckup:`t.trim_price != null` 的行,主行下追加 colspan=6 子行(leglist):半仓 trim_date 以 trim_price 先卖(T1)+该腿收益、半仓 sell_date 以 sell 卖出(still_open 时文案"仍持有,按最新收盘计")+该腿收益、整笔加权扣费 0.15% = pnl_pct
- [ ] test_web:T13C 转绿

### Task 6: 三测 + E2E(preview 8788)

- [ ] `.venv/bin/python test_rules.py && test_rules_mid.py && test_web.py` 全绿(8/20/379)
- [ ] E2E:输 cckj → 候选出长川科技 → 点选 → 一条龙 + 回放表子行两笔明细;输 300604 直查同效;输 gzmt 回车唯一候选直查;IME 守卫不回归;375px 下拉与子行不溢出;console 零错误

### Task 7: 交付总结 + 文档

- [ ] `docs/交付总结-13-腿明细与搜索-20260710.md`;CLAUDE.md 进度行;MEMORY.md 追加决策行;全局会话交接记忆更新

## Self-Review

- 拍板三点全部落在 Task 1/2(重跑)与 Task 5(子行/联想规则);
- 类型一致:trim_date/trim_price 名称贯穿 CSV→service→前端;get_stock_directory 返回键 code/name/spell 与 T13A/端点一致;
- 老 CSV 兼容:_stock_replay_of 用 r.get() 兜底,新 CSV 未就位时前端 trim_price==null 走单行,无破坏。
