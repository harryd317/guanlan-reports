# 产业温度计与产业档案页验收报告

- 日期：2026-08-02
- 分支：`codex/industry-thermometer-archives`
- 实现 HEAD：`9247701`
- 边界：纯展示，零交易；未改策略、风控、选股、交易和收盘任务
- 状态：代码与生产等效双视口验收已通过；仍有随附原文缺失和一条主工作树回归误写的 S3 拒绝审计记录，因此整体不宣称全部完成

## 一、交付内容

1. 新增申万二级行业四灯温度计，合成冷、温、热、烫四档，缺数单独显示“待更新”。
2. 股票池新增第三子页签“产业档案”，支持行业药丸过滤、左侧列表、右侧 Markdown 阅读和深链。
3. 档案每次请求扫描 `docs/产业档案/*.md`，新提交文件无需登记。YAML 头支持单行、逗号、内联列表和多行列表。
4. Markdown 以 CommonMark+表格渲染，原始 HTML 禁用，请求 ID 只能反查已扫描目录，阻断路径穿越。
5. 个股在计算日的申万二级行业与档案 `industry` 相交时显示档案入口，无匹配则整行隐藏。
6. 只新增三个 GET API：`/api/industry-thermometer`、`/api/industry-archives`、`/api/industry-archives/{archive_id}`。

## 二、计算口径与现场修正

真实 T1 全量库首跑时，“最新共同日”测试失败。调试确认：

- `cluster_daily` 与 `daily_vol` 覆盖 2015-01-05 至 2025-12-31；
- T1 中 `warm_daily`/`warm_adj_factor` 只覆盖 2013-12-02 至 2014-12-31；
- 既有 `market.db` 的 `market_daily`/`adj_factors` 覆盖 2015-01-05 至 2026-07-31。

因此补了独立行情库失败测试，然后做最小修正：集群灯、量比灯和历史成员继续只读 T1；成交占比与 120 日 RS 只读复用既有 `market.db`。计算日取两库四表共有的最新交易日。未新增数据源、定时任务、表或写路径。

全量结果：

| 项目 | 结果 |
| --- | ---: |
| 数据日 | 2025-12-31 |
| 行业总数 | 134 |
| 四项完整行业 | 131 |
| RS 有效股票 | 5,409 |
| 冷 / 温 / 热 / 烫 / 缺数 | 20 / 83 / 23 / 5 / 3 |

## 三、生产等效环境

- 生产 LaunchAgent 原文件一字未改，克隆标签为 `com.rui.stock-screener.web.industry-archives-equivalent`。
- 克隆工作目录为本分支工作树，启动脚本仍为同一 `start.sh`，端口 `19787`。
- `screener.db`、`market.db`、`S3_research.db` 使用 SQLite 一致性只读副本；完整 T1 库以 SQLite `mode=ro` + `query_only=ON` 打开。
- `/api/health` 返回 `ok=true`、`match=true`，启动/磁盘指纹都为 `9b2db1af10049e486be89fb6e79c5e607e5ec424`。
- 向 `/api/account` 发送写探针返回 403。
- 验收结束后已卸载克隆 LaunchAgent，`19787` 无监听。

等效验收启停窗口内，所有哈希完全相等：

| 对象 | SHA-256 |
| --- | --- |
| 生产 plist | `93e862839f8a54ee25d2aefc704742eb82ba106cd7b5239342b5245fc2a42dac` |
| 生产 screener | `ecfb06f626a5003518c335dbbb919469bb700e6398d34a68540e3a74e5f2f370` |
| 生产 market | `a504f873eb204bee722082b1d6fec92b04e950cbae16842d4c4917a820c75fc1` |
| 生产 S3 | `bf7aef744b05a69a16416c32f10663865a798f77ad828f93de54e6421018fb7b` |
| 副本 screener | `94c99dbfaaeaa507362d95837cdd7ee2b9c7f403adfca45d690706582f5894ff` |
| 副本 market | `4ee29aa68fdc116bec07de56a1b258eab3437e87568aadfa1f2d84d46bd9ca1c` |
| 副本 S3 | `e38deb016eb48bdabcb5113d1f5198d8c8eb82fc0e4d7fd76ff883275bdaedd1` |
| 完整 T1 | `111f2017d476e87dfdc68d3541d7678630d466d410a91f4ab37d8bcb9ce049d5` |

## 四、双视口视觉与交互

| 验收项 | 桌面 1440×900 | 手机 390×844 |
| --- | --- | --- |
| 产业档案页 | 通过 | 通过 |
| 文档横向溢出 | 0 | 0 |
| 文档纵向溢出 | 0 | 0 |
| 134 个行业名 | 全部显示 | 全部显示 |
| Markdown 表格 | 6 张正常渲染 | 阅读区单列 |
| 控制台错误 | 0 | 0 |

交互实测：

- 点“半导体”药丸后，列表从 2 篇过滤为唯一匹配档案。
- 通富微电 `002156` 显示“产业档案”链接，点击后深链到硬件链档案。
- 大族激光 `002008` 无匹配档案，链接行完全隐藏。

截图位于 `docs/screenshots/industry-archives-equivalent/20260802/`：

- `产业档案-桌面.jpg`
- `产业档案-手机.jpg`
- `产业档案-半导体过滤-桌面.jpg`
- `产业档案-表格阅读-桌面.jpg`
- `个股产业档案入口-桌面.jpg`
- `个股产业档案入口-手机.jpg`

截图尺寸和 SHA-256 见 `reports/industry-archives-equivalent-20260802.json`。

## 五、自动化验证

- 产业温度计、档案、API、新前端、原 API 基线和等效模式聚焦测试：53/53。
- `test_rules.py`：8/8。
- `test_rules_mid.py`：20/20。
- `test_web.py`：838/838。
- `test_regime.py`：6/6。
- `static/home.html` 两段 JavaScript 语法：2/2。
- `git diff --check`：无错误。

独立工作树首跑回归曾因三份未被 Git 跟踪的冻结基线文件缺失失败。从主工作树复制后逐份 SHA-256 一致，代码无需为此修改。

## 六、未满足项与生产只读事件

### 1. 随附原文缺失

用户要求的“随附《产业档案-存储与算力硬件链.md》”没有出现在当前任务附件、`Downloads` 或项目可读文件中。为避免伪造研究结论，已提交带正确 YAML 头的明确空态档案，状态为“待附件原文”。

### 2. 主工作树回归误写一条 S3 拒绝审计记录

最终复核发现，生产 `S3_research.db` 在等效验收结束后由 `bf7aef744b05a69a16416c32f10663865a798f77ad828f93de54e6421018fb7b` 变为 `9752050d5071e1fcfa3bc51aa5147053bdff19f898f787fcc4fd4356dc2a0a34`。时间戳和逻辑转储差异确认，这是此前为诊断独立工作树基线缺失而在主工作树运行 `test_web.py` 时产生的测试污染：

- `job_runs` 新增 id=8；
- `job_name=s3_incremental`，`trade_date=2026-06-07`，状态 `rejected`；
- `started_at=finished_at=2026-08-01T17:59:46.726261+00:00`；
- 错误为 `breadth rows are empty`、`clusters rows are empty`；
- 除该行及 `sqlite_sequence(job_runs)` 从 7 变 8 外，两库逻辑转储逐行一致。

等效实例始终使用副本和只读连接，启停窗口内生产库哈希一致；这条记录来自等效验收后的主工作树回归误操作。由于用户明确要求生产库只读，本任务没有再写生产库擅自删除。后续须由用户授权后，精确删除 id=8 或以验收副本恢复，并再次记录哈希。

在随附原文补齐且上述测试污染获得授权修复前，本报告不把整体任务表述为“已完成”。代码、只读计算、档案基础设施、个股深链和生产等效双视口验收均已通过。

## 七、镜像闭环

本报告、设计、计划、两篇档案、证据 JSON 和六张截图须先提交到本分支，再通过 HTTPS 同步报告镜像并从全新克隆验证。最终可查镜像提交哈希在任务最终回报中给出。
