# T7 生产重复部署失败与回滚报告

- 日期：2026-08-01
- 用户授权：T7 合并 main 并重新部署；五页双视口、数据库哈希和健康检查任一异常立即回滚
- T7 分支：`codex/t7-today-two-lines`，`f5ba70533ff444e44a3c4288d9acdd7157ed7cfe`
- T7 merge：`230daa6cf78837d14a961d3c4fb4d426b2cd7f2b`
- 部署前与部署时 main：`8d96e39282fbb89ef23cfa1243024964cf27ca58`
- 生产 PID：`35752` → `45097` → 回滚后 `45869`
- 最终判定：**本次重复部署未通过；已按授权回滚，不得报告部署完成**

## 结论

T7 在收到重复部署授权时已经合并 main，`main` 与 `origin/main` 均为 `8d96e39282fbb89ef23cfa1243024964cf27ca58`，因此没有制造重复 merge 提交。本轮在生产工作目录执行 `./restart.sh`，生产进程由 PID `35752` 重启为 `45097`。

重启后的健康检查通过，五个 HTTP 路由均为 200，生产五页桌面 `1440×900` 和手机 `390×844` 共十张视口截图也通过目视和机器合同。但是十图访问后的最终哈希复核发现：`screener.db` 从 `8c52821caefacc0134e25f36ee7071d6421ae33f48bff5fab45c3212e10fe839` 变为 `ece7a893cbd61fb788da7b6850630524caef877b62d1e2fd4cf1ddf15076b8b9`。该变化违反本次“数据库哈希部署前后一致”的硬门禁。

发现后立即再次执行 `./restart.sh`，回滚到部署前代码版本。由于本轮部署前后本来就是同一个 `main` 提交，代码回滚目标仍为 `8d96e39282fbb89ef23cfa1243024964cf27ca58`。回滚后生产 PID 为 `45869`，健康检查 `ok=true`、`match=true`。没有覆盖或恢复数据库文件，异常后的 `screener.db` 哈希仍为 `ece7a893…`，因此本轮不能判为通过。

## 部署与回滚时间线

| 阶段 | 结果 |
| --- | --- |
| 部署前 | main/origin main 均为 `8d96e39…`；PID `35752`；健康 `ok=true` |
| 重启 | 在生产工作目录执行 `./restart.sh`；PID 变为 `45097` |
| HTTP 冒烟 | `/`、`/pool`、`/trade`、`/strategy`、`/settings` 均为 200 |
| 五页验收 | 今天/股票池/我的/工程模式/设置，桌面与手机十图均为新界面，无横向溢出 |
| 异常 | `screener.db` SHA-256 变化，文件修改时间为 `2026-08-01T02:40:01+08:00` |
| 回滚 | 立即执行 `./restart.sh` 回到部署前同一代码提交；PID `45869` |
| 回滚后 | 健康 `ok=true`、`match=true`；数据库文件未被覆盖，异常哈希保留 |

## 健康检查

重启后和回滚后均返回：

```json
{"ok":true,"fingerprint_boot":"9b2db1af10049e486be89fb6e79c5e607e5ec424","fingerprint_disk":"9b2db1af10049e486be89fb6e79c5e607e5ec424","match":true,"rule_version":10}
```

## 数据库与 plist 哈希

| 对象 | 部署前 | 十图后 | 结果 |
| --- | --- | --- | --- |
| 生产 plist | `93e862839f8a54ee25d2aefc704742eb82ba106cd7b5239342b5245fc2a42dac` | 同左 | 一致 |
| `S3_research.db` | `bf7aef744b05a69a16416c32f10663865a798f77ad828f93de54e6421018fb7b` | 同左 | 一致 |
| `bt_cache.db` | `d8679bfe45aa350d272b24bb3120a78f4b722a6948a148973bb8899bb78ef47e` | 同左 | 一致 |
| `market.db` | `a504f873eb204bee722082b1d6fec92b04e950cbae16842d4c4917a820c75fc1` | 同左 | 一致 |
| `market.db-shm` | `fd4c9fda9cd3f9ae7c962b0ddf37232294d55580e1aa165aa06129b8549389eb` | 同左 | 一致 |
| `market.db-wal` | `e3b0c44298fc1c149afbf4c8996fb92427ae41e4649b934ca495991b7852b855` | 同左 | 一致 |
| `screener.db` | `8c52821caefacc0134e25f36ee7071d6421ae33f48bff5fab45c3212e10fe839` | `ece7a893cbd61fb788da7b6850630524caef877b62d1e2fd4cf1ddf15076b8b9` | **不一致，触发回滚** |
| `screener.db-shm` | `fd4c9fda9cd3f9ae7c962b0ddf37232294d55580e1aa165aa06129b8549389eb` | 同左 | 一致 |
| `screener.db-wal` | `e3b0c44298fc1c149afbf4c8996fb92427ae41e4649b934ca495991b7852b855` | 同左 | 一致 |

机器证据：

```text
reports/t7-production-redeploy-20260801/before-hashes.json
reports/t7-production-redeploy-20260801/after-hashes.json
reports/t7-production-redeploy-20260801/rollback-hashes.json
```

## 五页双视口证据

截图目录：

```text
docs/screenshots/t7-production-redeploy/20260801/
```

| 页面 | 桌面 | 手机 | DOM 横向溢出 | 目视结果 |
| --- | --- | --- | ---: | --- |
| 今天 | 1440×900 | 390×844 | 0 | 新界面正常，无空白 |
| 股票池 | 1440×900 | 390×844 | 0 | 新界面正常 |
| 我的 | 1440×900 | 390×844 | 0 | 新界面正常，无挤压 |
| 工程模式 | 1440×900 | 390×844 | 0 | 展开正常，DOM 确认 7 个入口 |
| 设置 | 1440×900 | 390×844 | 0 | 新界面正常，无挤压 |

十张图片都是真实 JPEG 视口截图，像素尺寸精确匹配视口。逐图 SHA-256、DOM 尺寸和页面映射位于：

```text
reports/t7-production-redeploy-20260801/visual-evidence.json
```

视觉验收本身通过，但不能覆盖数据库哈希门禁失败，故整次部署判失败。

## 异常边界

本轮只确认文件级事实：`screener.db` 的修改时间与手机“我的”页面取证时间同为 `02:40:01`，最终哈希随之变化。当前证据不足以确定是哪一个读取链、SQLite 初始化动作或并发生产任务造成变化；本报告不把相关性写成根因。

后续若再次部署，应先建立可比较的 SQLite 内容级快照或审计证据，在生产等效环境稳定复现该写入，再以 TDD 做最小修复。未经新的用户授权，本轮不继续修改生产代码或数据库。

## 完成定义状态

失败/回滚报告、哈希证据和十张截图需提交，并同步到远端镜像后回报镜像提交哈希。本动作只完成事故闭环，不改变“本次部署未通过”的判定。
