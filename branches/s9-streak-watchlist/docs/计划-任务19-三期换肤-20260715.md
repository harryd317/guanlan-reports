# 任务19 · 全站重设计三期(问AI/交易台换肤)实施计划

> **执行方式:** 子代理TDD逐任务+双审+终审,同任务17/18流程。
> **视觉依据:** 一期拍板原型 docs/原型-全站重设计-方案AB-今日驾驶舱-20260714.html 的屏3(问AI)/屏4(交易台)+ 已上线的 home.html/pool.html 活样板(设计令牌以这两页为准)。**纯换肤免新原型**(项目规矩:结构与交互零变化)。

**目标:** ask.html 与 trade.html 的视觉层统一到新设计语言(令牌/卡片/按钮/字阶/间距),**结构、交互、JS 逻辑、文案零改动**。

**架构:** 每页只动 `<style>` 块与必要的 class 补充(允许给既有元素加 class,不许删改 id/文案/DOM结构/JS);零后端改动,零重启。

## 全局约束(同任务17/18,另加)

- 判定链四文件+strategies/*零改动;test_web 既有用例只加不改;铁律6
- **换肤铁则:JS 一行不改、可见文案一字不改、DOM 结构不增删**(唯一例外:元素 class 属性可追加;确需包一层容器实现布局时,先 grep 确认无锁再动,并在报告里逐处列明)
- 设计令牌照 home.html/pool.html 现行 :root 原样(--bg/--card/--line/--txt/--mut/--blue/--up红涨/--down绿跌/--amber/--red/--green/--r-lg 20/--r-md 14/--r-sm 9/--shadow;.num 等宽)
- 按钮收敛:主按钮黑底(.btn.pri 同款)/次按钮白底描边/警示按钮红字描边;卡片圆角 20/阴影同款;tag/badge 样式对齐 pool.html
- grep 守卫:动手前 `grep -n "_src_ask\|ask\.html" test_web.py`、`grep -n "_src_trade\|trade\.html" test_web.py`,逐条核对(T12 系列钉 ask,TP0F/TP2/TP3/TPL 系列钉 trade),被钉字符串不许变
- trade.html 含并行会话 07-15 刚交付的 TPL 区块(阶梯影子台账/重申卡闸门/熔断容量),**只给它们套新样式,内容零动**
- 大文件施工铁则:分块小步 Edit(单次≤150行),步间跑 test_web 尾行
- 提交外科手术式(git add 指定文件),防再有并行改动
- 静态文件免重启;E2E 禁改库(不点会落库的按钮)

---

### 任务19-1: ask.html 换肤

**Files:** Modify `static/ask.html`(仅样式层);Test `test_web.py` ㉙节 T19A×3
**要点:** 三分区chips/课堂票签/三段课文卡/K线容器/查任意票搜索与体检卡/随便问对话区,全部套新令牌;顶部已是统一导航(17-1),页内旧 .topbar/.tab 残留死CSS一并清
**测试:** T19A1 新令牌标记在(`--r-lg:20px` 且 `--shadow` 在 ask.html);T19A2 三分区结构锚全存活(我的课堂/查任意票/随便问 + T12 关键 id,grep 现有锁核对后选3个代表断言);T19A3 JS 哨兵:`ask.html` 的 `<script>` 段与改前逐字节一致(实现法:动手前 `python3 -c` 抽出 script 段存 sha1 进测试注释,断言当前 sha1 相等——若此法与"只加不改"框架冲突,退化为断言关键函数名清单齐全)
**步骤:** grep守卫→落T19A(RED)→分块换肤(令牌:root→组件类→死CSS清理)→test_web零回归→浏览器自查(桌面+375px)→commit `任务19-1: 问AI页换肤`

### 任务19-2: trade.html 换肤

**Files:** Modify `static/trade.html`(仅样式层);Test `test_web.py` ㉙节 T19B×3
**要点:** 账户卡/持仓(纪律仓/场外仓分区)/统计卡(验证期20格/回滚线)/净值/交易日志/迁移卡/OCR批量识别卡/TPL新区块(影子台账/重申卡/容量公式),全部套新令牌;20格验证期小方块沿用红盈绿亏语义色
**测试:** T19B1 新令牌标记在;T19B2 结构锚存活(TP0F/TP2/TP3/TPL 锁 grep 后选3代表);T19B3 JS 哨兵(同 T19A3 方法)
**步骤:** 同19-1,commit `任务19-2: 交易台换肤`

### 任务19-3: E2E 验收 + 交付

- 四测终跑;浏览器 E2E(禁改库):/ask 三分区切换/课堂选票渲染/搜索联想框形态;/trade 全区块渲染+TPL区块无破版;两页 375px;console 零错误;截图汇报
- CLAUDE.md 补任务19段;`docs/交付总结-19-三期换肤-20260715.md`(遗留清单延续);commit+push
