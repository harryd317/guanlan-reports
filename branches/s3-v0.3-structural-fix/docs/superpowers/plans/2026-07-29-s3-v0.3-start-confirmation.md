# S3 v0.3 结构修正｜动工确认

> 最终状态（2026-07-29）：三项修正、81 组、第三次且最后一次样本外、
> S2 对照、报告和账本封存均已完成。G1 不通过，S3 按预声明无限期暂停；
> 未合并、未部署、未接生产、未动 T5，禁止 v0.4 或变相调参。

## 1. 基线与冻结项

- 分支：`codex/s3-v0.3-structural-fix`；基线：v0.2 封存提交 `804e4da`。
- 预声明：`s3-cluster-v0.3-structure-fix-2026-07-29`。
- 81 组网格及完整 SHA-256
  `aa380046884ea99c3103a68decc51945cf730de5fce01ccb6bde59ea1bedb18e`
  不变；样本内 2015—2021、样本外 2022—2025 不变。
- 选参仍为：未四舍五入最大回撤严格 `<25%` 过滤，再选未四舍五入年化超额最高，
  并列取回撤小；完全并列停机；无组合过线运行预声明基准。
- v0.2 的信号日财务/ST/申万二级资格重检、账本强封存及事件审计全部保留；
  捕获规则、ATR 两类买点、仓位公式、排序权重、费用和 S2 对照口径不变。
- 2022—2025 样本外只允许第三次且最后一次运行；G1 失败即无限期暂停 S3，
  禁止 v0.4、追加参数、补跑组合或其他变相调参。

## 2. 计划修改文件

- `s3_backtest/config.py`：仅更新 v0.3 预声明 ID；冻结网格和日期常量。
- `s3_backtest/portfolio.py`：用风险档位状态机替代停新仓状态机。回撤相对同一峰值
  `>=8%` 时单笔计划风险为 `0.5%`，`>=12%` 时为 `0.25%`；继续开新仓；
  未回到回撤前峰值不得升档，回峰值恢复 `1%`。只改变计划风险倍率，不改既有仓位公式、
  仓位上限、退出规则，也不强制卖出存量仓。
- `s3_backtest/data.py`：读取 `breadth_daily.eligible_n/nh250`，在既有门上叠加
  `nh250 / eligible_n > 0.10` 强制关门；捕获时活跃集群过滤不变；信号日仍计算并审计
  `cluster_active`，但从否决原因移除，财务/ST/申万二级缺失或不合格仍否决。
- `s3_backtest/metrics.py`：把降档/恢复事件纳入确定性结果与封存核验，不改变选参指标算法。
- `s3_backtest/report.py`：生成 v0.3 全新制品、三段已知亢奋期核验、
  `NH/eligible` 全区间分布、全部过热门日期、档位事件、cluster_inactive 放行结果及
  v0.3/v0.2 逐笔和逐年对照。
- `s3_backtest_cli.py`：只指向全新的 `S3_v0_3_prepared.pkl`、
  `S3_v0_3_backtest_runs.db` 和 v0.3 报告名；沿用 v0.2 的只读核验与强封存流程。
- `test_s3_backtest.py`：先添加 R1/R2/R3 失败测试，再做最小实现；旧测试中仅替换被
  v0.3 明确废止的停机式熔断断言，其余回归保留。
- 新建且不覆盖：
  `reports/s3_v0_3_backtest_2015_2025.md`、
  `reports/s3_v0_3_parameter_grid.csv`、
  `reports/s3_v0_3_backtest_trades.csv`、
  `reports/s3_v0_3_blocked_entries.csv`、
  `reports/s3_v0_3_risk_tier_events.csv`、
  `reports/s3_v0_3_overheat_gate_events.csv`、
  `reports/s3_v0_3_signal_distribution.csv`、
  `reports/s3_v0_3_comparison.md`，以及最终 G1 验收报告。

`s3_backtest/ledger.py`、`s3_backtest/selection.py`、`s3_backtest/signals.py`、
`s3_backtest/s2_adapter.py` 预计无需修改；若实现现实迫使修改，将先停下报告，不自行变通。

## 3. 明确不动

- v0.1/v0.2 报告、CSV、账本与缓存不覆盖、不重写、不删除；动工前后逐文件 SHA-256
  比较，任何变化立即停机。
- 不改 `market.db`、`screener.db`、`S3_research.db`，研究快照只读；不读取
  `pool.industry`。
- 不改 S2、生产风控、回测核心、行情管道、收盘任务、页面、依赖清单。
- 不合并、不部署、不接生产、不动 T5；预声明所述影子/生产统一机械规则只记录为后续约束，
  本分支不实施影子或生产接入。

## 4. 验证设计

| 规则 | 先行测试与独立复核 |
|---|---|
| R1 风险降档 | 精确测试 `7.999% / 8% / 11.999% / 12%`；验证档位 `1%→0.5%→0.25%`、继续开仓、未回原峰不得升档、恰好回原峰恢复、多个周期事件有序且可复算；报告检查 2016—2021 成交 `>0`，并排查每个整年零成交是否存在异常停摆。 |
| R2 过热保护 | 用同一时点 `nh250/eligible_n` 测试恰好 `10%` 不关、严格 `>10%` 关；`eligible_n<=0` 或字段缺失不得静默放行，必须停机；验证既有门条件未变、阈值未进入 81 组；全区间分布和触发日逐日独立复算，并单列 2015-04—06、2020 下半年、2024-09—10。 |
| R3 复核降权 | 捕获日非活跃仍不得进入候选；信号日仅 `cluster_inactive` 时必须放行并保持原排序字段，财务坏/缺失、ST、成员缺失仍逐项拦截；v0.2 `cluster_inactive` 拦截键与 v0.3 放行键做集合对照及结果分布。 |
| 封存与对照 | 新账本运行前后均为 81 个唯一参数；选参后只运行一次样本外和一次 S2 对照；封存后写入真实拒绝，内容哈希复验通过；v0.1/v0.2 制品哈希逐字节不变。 |

## 5. 验收自查表

- [x] 只实现 R1/R2/R3，参数网格、切分、选参规则和 v0.2 三项成果无漂移
- [x] R1 边界、继续交易、回峰恢复、多周期事件测试与独立复算通过
- [x] 2016—2021 合计成交大于 0；所有整年零成交均完成异常停摆审计
- [x] R2 严格大于 10%、零分母停机、三段亢奋期和全触发日复算通过
- [x] R3 捕获门槛不变、信号日集群不否决、其余三类资格继续否决
- [x] v0.2 cluster_inactive 拦截的放行数量和结果分布完整
- [x] 样本内恰好 81 组，冻结选参无变化；样本外第三次且最后一次、仅一行
- [x] G1 四项标准逐项给出结论，失败不补救、不重跑、不产生 v0.4
- [x] v0.3/v0.2 交易增减、逐年数量、共同交易结果差异及 S2 对照完整
- [x] v0.1/v0.2 全部制品 SHA-256 不变，账本强封存、禁改文件和零新增依赖验证通过
- [x] 分支未合并、未部署、未接生产、未动 T5

收到用户明确回复“动工”后，才按测试先行顺序修改实现、运行 81 组及最后一次样本外；
完成后提交在本分支并停止，等待 G1。
