# T2 动工确认：S3 全量回测

## 任务、授权与分支

- 任务：T2 阶段二——S3 全量回测、81 组样本内扫描、冻结选参、一次样本外验证、S2 同口径对照和 G1 报告
- 用户验收 T1：2026-07-29，明确确认 T1 验收通过并授权合并
- 用户授权 T2：2026-07-29，明确回复“合并后授权 T2 动工”
- T1 已接受提交：`7ed96fb37513f2e9b96b87b3d17a9a586f5dee35`
- T1 合并提交：`31cecb9`
- 工作分支：`codex/s3-stage2-backtest`
- 分支基线：严格从 T1 已接受提交 `7ed96fb` 建立
- 本次授权范围：只允许 T2 分支内开发、测试、封存研究运行和生成报告
- 停止点：提交 T2 报告后停在分支，等待闸口 G1；不得合并、部署、接入生产或动工 T5

## 冻结输入

- 生产行情库 `market.db`：只读打开；整库 SHA-256 为
  `6cacb56e4b5eaf7a97aa28e6d5d278164472a13bd9131f86abfc23e6138e1da5`
- T1 研究库 `S3_research.db`：只读打开；整库 SHA-256 为
  `3e84ba621f889151e7c19ceea3b31ff7888c6bb9ec081877aefc2ce25553e357`
- T1 公式版本：`s3-stage1-v1`
- 预声明 ID：`s3-cluster-v0.1-2026-07-28`
- 样本内：2015-01-01—2021-12-31
- 样本外：2022-01-01—2025-12-31
- 参数空间：固定 `3×3×3×3=81` 组，不增不减
- 选参规则：先严格过滤最大回撤 `<25%`，再取未四舍五入年化超额最高，并列取回撤较小；无组合过线则使用预声明基准

任何整库指纹、表级输入指纹、公式版本、预声明 ID 或参数网格哈希变化，都必须拒绝沿用本次运行，不得覆盖、删除或从头重跑已产生结果。

## 将创建

- `s3_backtest/__init__.py`：预声明版本和公开接口
- `s3_backtest/config.py`：冻结参数、区间和 81 组网格
- `s3_backtest/selection.py`：冻结选择纪律
- `s3_backtest/ledger.py`：独立运行账本、断点续传和封存
- `s3_backtest/data.py`：只读 T1/生产数据适配、时点可见性和输入指纹
- `s3_backtest/signals.py`：资格、机械排序和两类入场
- `s3_backtest/engine_adapter.py`：只调用既有成交与费用帮助函数
- `s3_backtest/portfolio.py`：组合、仓位、退出和权益事件循环
- `s3_backtest/metrics.py`：收益、回撤、交易和信号统计
- `s3_backtest/s2_adapter.py`：S2 同口径调用适配
- `s3_backtest/report.py`：八项报告、数据审计、局限和五笔复核
- `s3_backtest_cli.py`：样本内、锁参、一次样本外和报告命令
- `test_s3_backtest.py`：冻结纪律、无前视、成交、确定性、对照和报告测试
- `S3_backtest_runs.db`：独立本地运行账本；不进入 Git，不接触生产库
- `reports/s3_backtest_2015_2025.md`
- `reports/s3_parameter_grid.csv`
- `reports/s3_backtest_trades.csv`
- `reports/s3_signal_distribution.csv`
- `docs/验收-T2-S3回测-20260728.md`

## 明确不动

- 生产策略：`rules_mid.py`、`strategies/`、`screener.py`
- 风控：现有仓位、熔断、开仓门和服务端拦截
- 回测核心：`backtest.py`、`backtest_mid.py`、`backtest_defense.py`
- 生产数据管道：`market_data.py`、`data.py`、现有 EOD 数据处理步骤
- 定时任务：`eod.py`、`schedule.sh`、`run_eod_trigger.sh`
- 服务和生产界面：`service.py`、`static/`
- 生产数据库：`market.db`、`screener.db` 及任何现有表结构、索引和数据
- T1 研究输入：`S3_research.db` 的表结构和数据
- 生产存储层：`storage.py`
- 参数：`params.py`
- 依赖：`requirements.txt`，不安装 npm/pip 包
- 行业口径：不读取、不映射 `pool.industry`
- T5：不创建影子账本、不接收盘任务、不生成真实买卖提示

## 验收自查表模板

- [ ] 基线严格为 T1 已接受提交 `7ed96fb`
- [ ] `market.db` 和 `S3_research.db` 全部以 `mode=ro` 打开
- [ ] 整库、表级、公式版本、预声明和参数网格指纹完整
- [ ] 生产库、T1 研究库在运行前后逐字节指纹一致
- [ ] 参数组合恰好 81 组且唯一
- [ ] 最大回撤必须严格 `<25%`，`25.0%` 不通过
- [ ] 选参只按冻结规则，完全并列时停止，不增加第三排序键
- [ ] 样本内不读取 2022 年及以后的数据
- [ ] 样本外只运行一次，不反馈选参
- [ ] 财务按实际公告日使用当时已公开年报
- [ ] 2015 ST 缺口、成分历史与幸存者偏差如实报告
- [ ] 暖窗只用于初始化 2015 指标，不产生暖窗交易
- [ ] `daily.vol` 只用于深回调放量和 S2 同口径对照
- [ ] 全链路只用申万二级，零读取 `pool.industry`
- [ ] 费用、涨跌停、停牌调用现有回测帮助函数
- [ ] 2015—2025 共 11 个年度行
- [ ] 报告八项齐全并给出四项过闸结论
- [ ] 81 组每组输出期望和回撤，完成稳定参数岛检查
- [ ] 交易明细可抽查，固定种子复核 5 笔
- [ ] S2 对照区间、股票域、OHLCV、费用、板制、停牌和指数序列同口径
- [ ] 新增 T2 测试全部通过
- [ ] 既有 8/8、20/20、834/834 和市况回归不退化
- [ ] `git diff --check` 通过
- [ ] 禁区文件零差异
- [ ] 零新增依赖
- [ ] 无演示数据、硬编码股票、指数点位或回测结果
- [ ] T2 分支未合并、未部署、未接入生产
- [ ] G1 评审前不动工 T5

本确认仅记录用户已经给出的 T2 动工授权。它不授权合并、部署、生产接入或 T5。
