# 交付总结 · 任务21 全站重设计四期(低频页换肤)+ /pro 退役

**交付日期:** 2026-07-15 傍晚
**计划:** docs/计划-任务21-四期换肤与pro退役-20260715.md(原编号任务20,因并行会话"长线画像"撞名改21)
**用户拍板:** "接着把四期也做了,pro也可以退役了"——旧锁白名单修改授权
**提交链:** 53ae3e8(四页换肤)→ 09917a8(/pro退役+旧锁退休)→ cd19d7d(终审修复)
**四测终态:** 8/8 · 20/20 · **684/684**(㉝节 T21A×5+T21B×4)· 9/9;restart 指纹 match;curl /pro=307→/pool 实测

## 交付了什么

1. **四期换肤**:daily/history/settings/stock_detail 设计系统块 v3→v4(四页同一新块,令牌照 home/pool 逐字节,选择器集不变只换值),`header:not(.unav)` 收窄,断点 640→680,页专属硬编码旧色归一;ask 移动端 toast 提到底部Tab上方(三期遗留#2 销账)。五页 style 段外剥离 SHA1 前后一致(终审独立复算通过)。**至此全站十页全部并入新设计语言。**
2. **/pro 退役**:路由改 307 重定向 /pool(老书签不404);static/index.html 删除(-779行,功能已收编:盘中监控/筛选结果=pool三视图、AI二审=共享抽屉、今日待办=home/daily);全站 /pro 链接与六页 topnav 死DOM、home 齿轮遗产(gearpanel/saveAcct)清除。
3. **旧锁退休**(白名单,每处留拍板注释):T5F1→"topnav死DOM全站清尽"/T5F2+T7E3→退役完成断言/T7E1 去 /pro 条件/T10B1→"维修间无pro+账户编辑在交易台"/T10B2→"齿轮遗产已清"/T17A4 清单去 index.html;新锁 T21B×4(含 page_pro() 直调钉死 307+location)。

## 质量过程(本期特殊)

**本期由控制器 inline 实施**(用户连续拒绝子代理派工,遂改自做),没有任务级独立审查——因此 fable 终审按"唯一独立审查"从严执行,并真抓到 1 个 Important:**stock_detail 持有徽章被旧色归一成红,与 v4 全站"持有=绿"语义相反**(已修 cd19d7d);另抓 T21B1 断言弱于计划(已补直调 307)、死CSS/过时注释六处(已清)。终审三大从严点(白名单忠实性/退役完整性/换肤铁则)全部独立复证通过。

## 交付后生产事故(07-15 深夜,已修 74b753e)

用户打开首页白屏(只剩导航)。根因:cd19d7d 清过时注释的**跨行 `.*?` 正则从更早的 `<!--` 起点吞到目标注释**,误删 clockbar/warnbar/截图输入框/**`#main` 主容器**/reaffirm.js;字符串型守护断言全绿没拦住(id 字符串在 JS 里仍出现)。修复=从 09917a8 恢复+精确字面量删注释+纯注释防护;新增 T21B5 按「元素声明」钉死六个渲染关键节点。**教训入库:①清注释绝不用跨行通配正则 ②守护断言钉元素声明而非裸字符串 ③每次交付后必须真实打开受影响页面看一眼(本次恰好漏了首页)**。

## 遗留清单

1. 万/亿快捷单位输入随齿轮退役消失(该UI自17-3起本已不可见);如需恢复,在交易台账户编辑器上重做(小活)
2. 站点级「按钮体系统一」(消息卡动作族蓝底 vs primary黑底)+「amber 双源归一」(home/ask warnbar 硬编码)——历史遗留,与本期无关
3. T7E1 的改动超出计划白名单一项(方向正确、有拍板注释,终审判留档即可)
4. 换肤报告 SHA1 附剥离脚本的流程改进已在本期落实

## 全站重设计总账(任务17→21 完整收官)

- 一期:统一导航 nav.js + 首页驾驶舱
- 二期:股票池一页三视图 + AI 二审共享抽屉
- 三期:问AI/交易台换肤
- 四期:四低频页换肤 + /pro 退役 + 旧锁退休
- 全站十页一套设计语言;简明版/专业版双世界彻底合一;测试 551→684(净增133条);历次 fable 终审全过
