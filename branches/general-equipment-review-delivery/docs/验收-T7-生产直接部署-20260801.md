# T7 生产直接部署验收报告

- 日期：2026-08-01
- 用户授权：直接部署，不再以数据库原始文件哈希一致作为本次门禁
- 部署源码提交：`2118f42d53cf390885002727d646e81339b02eaa`
- T7 merge：`230daa6cf78837d14a961d3c4fb4d426b2cd7f2b`
- 部署方式：生产工作目录执行 `./restart.sh`
- 生产 PID：`45869` → `52221`
- 结果：部署成功，未回滚

## 结论

按用户最新授权，本次明确豁免 `screener.db` 原始文件 SHA-256 部署前后一致门禁。当前 main 已通过生产原有 LaunchAgent 和 `restart.sh` 重启，健康检查返回 `ok=true`、`match=true`，启动代码与磁盘代码指纹均为 `9b2db1af10049e486be89fb6e79c5e607e5ec424`，规则版本为 `10`。

`/`、`/pool`、`/trade`、`/strategy`、`/settings` 均返回 HTTP 200。当前 PID 下重新采集生产五页桌面 `1440×900` 与手机 `390×844` 十张普通视口截图，目视全部为新界面；DOM 横向溢出为零，工程模式桌面和手机均确认 7 个入口。

数据库哈希从此前记录的 `ece7a893cbd61fb788da7b6850630524caef877b62d1e2fd4cf1ddf15076b8b9` 变为 `d3c201779835a7b65fdff70dbf25c0ab84e824786e7367f39d34f56410ecfac1`。该变化如实记录，但因用户本次明确豁免，不触发回滚。

## 运行验证

```json
{"ok":true,"fingerprint_boot":"9b2db1af10049e486be89fb6e79c5e607e5ec424","fingerprint_disk":"9b2db1af10049e486be89fb6e79c5e607e5ec424","match":true,"rule_version":10}
```

| 路由 | HTTP |
| --- | ---: |
| `/` | 200 |
| `/pool` | 200 |
| `/trade` | 200 |
| `/strategy` | 200 |
| `/settings` | 200 |

## 五页双视口

| 页面 | 桌面 | 手机 | 横向溢出 | 结果 |
| --- | --- | --- | ---: | --- |
| 今天 | 1440×900 | 390×844 | 0 | 新界面正常 |
| 股票池 | 1440×900 | 390×844 | 0 | 新界面正常 |
| 我的 | 1440×900 | 390×844 | 0 | 新界面正常 |
| 工程模式 | 1440×900 | 390×844 | 0 | 新界面正常，7 个入口 |
| 设置 | 1440×900 | 390×844 | 0 | 新界面正常 |

证据目录：

```text
docs/screenshots/t7-production-direct/20260801/
reports/t7-production-direct-20260801/deployment.json
reports/t7-production-direct-20260801/visual-evidence.json
```

十张图片均为真实 JPEG 普通视口截图，未使用 `fullPage`，图像像素精确匹配声明视口。

## 说明

首次非提权健康复核曾因当前工具沙箱不能访问本机 8787 而返回连接失败。LaunchAgent 状态和 `lsof` 同时证明 PID `52221` 正在监听；随后使用与部署脚本相同的本机权限复核，健康接口和五路由全部通过，因此这不是生产掉线，也没有触发第二次重启或回滚。

工作树中原有未跟踪文件 `docs/AI评分校准-202608.md` 未纳入本次部署报告提交。
