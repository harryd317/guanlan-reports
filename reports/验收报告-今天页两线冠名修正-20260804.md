# 今天页两线冠名修正验收报告

- 日期：2026-08-04
- 分支：`ui/today-line-labels`
- 合并前实现提交：`b4333e7c91d4697ae2a4a320d8bb78ef5157adc5`
- 等效验收证据提交：`4c97eaa1fe120c346061ae5ff4cd28a5530205a6`
- 部署合并提交：`40c617ac3d63f95ab6ad4ce73b24c73507484286`
- 结论：通过，已合并 `main` 并重启生产。

## 1. 交付内容

今天页展示层完成以下修正：

1. `S8 关注` 改为 `投资线 · S8 关注`。
2. 新增副标 `产业核心股研究信号 · 供学习 · 非交易建议`。
3. `投机线 · 开仓门 / 市况` 改为 `市况 · 开仓门`。
4. 今天页不再把市场状态基础设施冠为投机线。
5. 股票池“选股逻辑”三张卡的数量、顺序和文案保持不变。

本批只改 `static/home.html` 的展示文案与为副标预留的样式空间；未修改数据接口、信号引擎、策略、风控、选股或交易逻辑。

## 2. TDD 与自动化回归

先增加 DOM 级失败断言，旧页面在 `投资线 · S8 关注` 断言处按预期失败；最小修改后转绿。新增断言同时锁定：

- 今天页三项新文案精确存在；
- 旧投机线冠名不再出现在今天页；
- 选股逻辑页三卡冻结顺序不变。

合并到 `main` 后重新执行全部规定门禁：

| 测试 | 结果 |
|---|---:|
| `test_guanlan_frontend.py` | 39/39 |
| `test_production_equivalent.py` | 27/27 |
| `test_rules.py` | 8/8 |
| `test_rules_mid.py` | 20/20 |
| `test_web.py` | 838/838 |
| `test_regime.py` | 9/9 |

## 3. 生产等效环境验收

按生产 plist 克隆配置启动独立端口 `18805`：

- 工作目录：功能分支 worktree；
- 启动脚本：同一 `start.sh`；
- 静态服务：同一应用服务方式；
- 数据库：`screener`、`market`、S3 sidecar、S8 四库均使用 SQLite 只读快照，权限 `0444`；
- 后台任务：等效模式关闭；
- 原生产 plist：一字未改。

结果：

- 健康检查：`ok=true`、`match=true`；
- 10 个冻结路由 × 2 个视口，共 20/20 个页面通过；
- 桌面 `1440×900` 与手机 `390×844` 均满足文档和活动页面 `scrollHeight === clientHeight`；
- 控制台错误：0；
- 今天页精确命中新标题、副标和 `市况 · 开仓门`，旧冠名缺席；
- 生产 plist、四个生产库及四个只读副本在等效启动前后哈希均一致。

证据：

- `reports/evidence/today-line-labels/equivalent/environment-hashes.json`
- `reports/evidence/today-line-labels/equivalent/global-zero-scroll.json`
- `reports/evidence/today-line-labels/equivalent/visual-manifest.json`
- `reports/evidence/today-line-labels/equivalent/today-desktop-1440x900.jpg`
- `reports/evidence/today-line-labels/equivalent/today-mobile-390x844.jpg`

## 4. 生产部署与验收

部署动作：将分支以合并提交 `40c617ac3d63f95ab6ad4ce73b24c73507484286` 合并到 `main`，运行生产 `restart.sh`。

健康检查：

- `ok=true`
- `match=true`
- `fingerprint_boot=a0e60fc76e51b5f3ba1410be419ec7e2d43ac094`
- `fingerprint_disk=a0e60fc76e51b5f3ba1410be419ec7e2d43ac094`
- `rule_version=10`

生产数据库与 plist 部署前后 SHA-256：

| 对象 | 部署前 | 部署后 | 结论 |
|---|---|---|---|
| 生产 plist | `4546524c15d5391442c3b74acaf567147f2fe12af6207c23fc3ca7bee9de01cc` | 同前 | 一致 |
| `screener.db` | `3e9eaa5c5501bfa4859cabaeb98d07951ab24ef77526e96bf549754405d27c1e` | 同前 | 一致 |
| `market.db` | `00a88007ac8a7ea31cafef9532d278dad644bde3daec6daf27664f999dac90a9` | 同前 | 一致 |
| S3 sidecar | `cc361059a7edf72379510b28d7ea47ae4fa91dc6027619c9ef8d2365ce77abca` | 同前 | 一致 |
| `S8_research.db` | `0d5023d63139a205d764ccc27d0c1525a08ec1485f75d0a2694afa82180b0911` | 同前 | 一致 |

上表是重启前与重启完成后的即时部署窗口，四库均未被部署动作改写。重启后生产按既有设计执行了一次 `_catchup_job`，因此 22:14 再观察时两个运行库正常演进：

- S3 sidecar 当前哈希：`89718e45a9421eb1b75795c72279938c91d595e3b2142cd8090c1cef9f0c0396`；温度计快照仍为 3 行，仅当日快照 `generated_at` 从 `13:53:28Z` 更新为 `14:14:11Z`。
- S8 当前哈希：`335cdfbb4f0215f36de349f0b3aa67ba1ef30b408cde62850f8c815212c2e6b1`；信号仍为 12 行、队列仍为 21 行、影子账仍为 0 行，仅队列 `updated_at` 更新至 `14:14:17Z`。
- `screener.db` 与 `market.db` 哈希仍与部署前相同。
- S3 sidecar 与 S8 均通过 `PRAGMA quick_check`，结果为 `ok`。

因此后续哈希变化来自生产重启后的正常闭环任务，不是本次静态展示部署污染；即时部署窗口的“前后一致”结论不变。完整记录见生产 `environment-hashes.json`。

生产视觉与零滚动结果：

- 10 个冻结路由 × 2 个视口：20/20 通过；
- 页面级横向、纵向溢出：0；
- 控制台错误：0；
- 桌面、手机今天页截图人工复核通过。

证据：

- `reports/evidence/today-line-labels/production/environment-hashes.json`
- `reports/evidence/today-line-labels/production/global-zero-scroll.json`
- `reports/evidence/today-line-labels/production/visual-manifest.json`
- `reports/evidence/today-line-labels/production/today-desktop-1440x900.jpg`
- `reports/evidence/today-line-labels/production/today-mobile-390x844.jpg`

## 5. 完成定义

本报告与全部证据提交后，由镜像同步钩子同步 `main` 的 `AGENTS.md`、`docs/`、`reports/`。任务最终状态以全新克隆镜像、逐项核对成功，并在交付回报中给出镜像提交哈希为准。
